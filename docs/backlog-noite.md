# Backlog priorizado — todo follow-up adiado da árvore `noite-baseline-2026-09-14`

Frente 1 da rodada noturna (task 1). Worktree `/home/marcelo/Projetos/rdna4-wt-noite-review`,
branch `feat/noite-review`. Base: `main` = tag `noite-baseline-2026-09-14` = `e48f3c4`.
Revisão que originou os itens marcados `rev-`: `docs/adversarial-noite.md`. Diário:
`docs/journal-review.md`. Regras: `docs/noite-regras.md`.

**O que este documento é.** (a) A extração exaustiva de tudo que está *documentado como adiado,
rejeitado, candidato, não verificado ou declinado* na árvore — de `docs/auditoria-qualidade.md` §7.3,
`docs/rocm-estudo.md` (ranking §E), `docs/vulkan-vs-hip.md` §4 (steal list), `docs/autotuning-gfx1201.md`
§4/§5, `docs/medicoes-banda-e-gargalos.md` §4/§6, `PLAN.md`, `SPEC.md`, `README.md` ("Known
limitations") e dos comentários que declaram limitação no código (a árvore **não tem** nenhum
`TODO`/`FIXME`/`XXX`/`HACK` — confirmado por grep —, então a dívida aparece em prosa); e (b) o
**re-ranking por ganho esperado ÷ custo**, com os itens desta frente no fim, marcados.

**Como a ordem foi feita.** Para o topo da lista (posições 1-48) eu escolhi a ordem explicitamente,
item a item, por (ganho esperado ÷ custo) — a coluna *nota de ordem* diz o critério de cada um. Do
49 em diante a ordem é por prioridade declarada (`P0` → `P3`) e, dentro dela, a ordem em que a
extração encontrou os itens; a prioridade de cada linha é a **do documento de origem** (ou a minha,
quando o item é meu), e **não** foi reescrita para caber na posição — um item P3 pode aparecer antes
de um P2 se o custo dele for quase zero e o ganho não. Onde um item é claramente o mesmo trabalho
que outro, os dois continuam na tabela (a linha que veio da extração e a minha) com a mesma nota.

**Colunas.**

| coluna | significado |
|---|---|
| `#` | posição no ranking por ganho ÷ custo (1 = melhor razão) |
| `id` | identificador curto; `new-` = follow-up implícito que ninguém escreveu como tal; `rev-` = **item desta frente**, no fim do arquivo |
| `item` | uma linha, imperativa |
| `pointer` | onde está documentado/pedido (`arquivo:linha` ou `doc §N`); linhas verificadas por grep/leitura |
| `ganho declarado` | o número que o documento dá, **como está escrito** (com a vírgula decimal e o hedge: "medido", "estimado", "sem numero"); não converti nem recalculei |
| `custo/risco` | esforço concreto e o motivo declarado de ter sido adiado, quando existe |
| `P` | `P0` correção/bloqueio · `P1` ganho medido de vários por cento · `P2` vale tentar · `P3` documentado e declinado (motivo em custo/risco) |
| `v` | `V` = ponteiro/afirmação **re-verificados por mim** nesta sessão (leitura + grep) · `M` = extração mecânica (com o grep da linha registrado no processo de extração, não re-lido por mim) |
| `nota de ordem` | por que o item está nessa posição (só para o topo da lista) |

**Prioridade zero (`P0`) declarada pela extração: nenhuma** — e isso é um resultado, não um buraco:
os quatro CRÍTICOS da auditoria (`E1`, `M7`, `M8`, `H1`) estão corrigidos **com prova antes/depois**,
que eu reproduzi por conta própria (ver `docs/adversarial-noite.md` F12). O único item que eu
classifiquei `P0` é meu: o gate que desaparece sem falhar (`rev-check-all-fail-on-missing-gate`),
porque ele pode fazer o relatório da manhã afirmar que a suíte de regressão passou quando ela não
rodou.

**Três avisos de leitura antes da tabela:**

1. **A lista de adiados da auditoria está velha em dois pontos**: a §7.3 (`docs/auditoria-qualidade.md:1108-1129`)
   ainda diz que `B1` (`-Wall -Wextra`) e `M2` (cauda do kernel split) foram adiados — **ambos já
   estão aplicados** nesta árvore (`CMakeLists.txt:38,40,246`; `include/rdna4/attn.cuh:395-407`).
   Em compensação `E2` (o `WPB` do kernel **sem** split é ignorado) **continua aberto**
   (`include/rdna4/attn.cuh:121,165,171,178`) e **não** foi re-medido. Ver `new-recheck-deferred-after-merges`.
2. **Ponteiros de documento envelheceram**: a auditoria cita `README.md:94`, `:132-138` (a reescrita do
   README acabou com essas linhas), `graph.cuh:315` (o `atoi` está em `:332`) e `rocm-estudo.md:417`
   (a linha do `kAttnSplitMin = 2048` é a `:429`). Cada linha da tabela traz o ponteiro verificado
   **na árvore atual**; onde o documento cita outra linha, isso está dito.
3. **Onze contradições entre documentos** estão listadas no fim deste arquivo. Duas delas mudam uma
   decisão de trabalho (`q3_k` +0,5% contra +1,7%; e o `MTP KV` de 8 MiB contra 16 MiB), e uma
   terceira é um número **retratado que continua sendo citado** (`18,91 tok/s` a 64K `f16`).

---

## 1. Tabela — 233 itens, do melhor ganho ÷ custo para o pior

<!-- gerado a partir da extracao bruta + reranking desta frente; ver docs/backlog-noite.md -->
| # | id | item | pointer | ganho declarado | custo/risco | P | v | nota de ordem |
|---|---|---|---|---|---|---|---|---|
| 1 | `q8-0-kv-default-above-56k` | Trocar o KV default a 64K+ para `q8_0` | docs/medicoes-banda-e-gargalos.md §3.3 linha 386, §4.4 linha 485 e §4.5 linha 503 | 19,87 ms contra 21,03 do q4_0; token inteiro 54,45 ms (18,36 tok/s) contra 55,02 (18,17); o dobro da precisao pelo mesmo tempo, com 2,18 GiB de folga | e a troca de uma linha no CLI/servidor e nao precisa de kv.h; CONTRADICAO interna: §3.3 diz "nao medido neste trabalho (ficou fora do orcamento de GPU; e o teste #1 da proxima sessao)" e §4.4 mede o mesmo caso | P1 | V | 1 linha no CLI/servidor; mata o penhasco de 8x do GTT |
| 2 | `new-bench-discount-in-code` | Implementar no codigo o desconto de `token_embd`/MTP que os docs ja tratam como correcao de registro | src/main.hip:373 (`bytes_per_token = loader.total_bytes()`) | 8% do numero de banda reportado | 1 linha mais ajuste das mensagens; rocm-estudo §C.9 e §A.2.6 pedem e quants-inventario §3.3 prova a aritmetica — os docs assumem que esta feito e nao esta | P1 | V | 1 linha; tira 8,2% de vies de todo numero de banda publicado |
| 3 | `kv-row-bytes-default-zero` | `kv_row_bytes` devolve 0 em silencio para um `KvType` novo | docs/quants-inventario.md §4 linha 262; include/rdna4/kv.h:74 | sem numero | `default: __builtin_trap()` ou `-Wswitch` (remove uma classe de falha silenciosa) | P2 | V | poucas linhas; remove a falha silenciosa que a frente KV vai encontrar hoje |
| 4 | `small-kernel-fusions-measured` | Fusoes medidas: `rms_norm`+`quantize` (130 lancamentos para 0,36 ms), as 4 ops escalares do GDN numa (144 para 0,40 ms), `kv_write` de 8 para 1 (112 para 0,31 ms) | docs/medicoes-banda-e-gargalos.md §4.3 linha 469 | ~1,1 ms/token (3%) medido, e nao estimado | kernel novo em nn.cuh/gdn.cuh por fusao (o estudo §E.2/E.3 ja traz o desenho); gate `check-graph-gpu` + golden para as fusoes bit-exatas | P1 | M | ~1,1 ms/token (3%) MEDIDO, 3 fusoes, ordem das contas preservada |
| 5 | `e2-wpb-ignored-unsplit` | O parametro de template WPB do kernel sem split e ignorado (4 pontos usam `kAttnWarpsPerBlock`) | docs/auditoria-qualidade.md §E2 linha 601; include/rdna4/attn.cuh:121,165,171,178 | sem numero; invalida o "0,73x a 4K e 1,07-1,19x a 8-64K" da varredura WPB sem split de docs/rocm-estudo.md linha 360 | corrigir 4 linhas + re-rotular e re-medir o sweep (adiado: mexe no kernel e a frente de autotuning estava no mesmo arquivo); ainda aberto nesta arvore | P2 | V | 4 linhas + re-medir; devolve validade a uma varredura publicada |
| 6 | `rev-check-all-fail-on-missing-gate` | check_all.sh deve FALHAR (ou imprimir SKIP e contar) quando o binario de um gate nao existe | scripts/check_all.sh:50 e :88 (provado movendo build/check-tuning) | sem numero: PASS com 9 de 10 gates, provado | 4 linhas de shell | P0 | V | gate fantasma: o relatorio da manha pode afirmar que a regressao passou |
| 7 | `mtp-kv-not-in-budget` | O KV proprio do `MtpHead` (duas caches, `d_ck_` e `d_cv_`) nao entra no orcamento de `info`/`serve` | docs/auditoria-qualidade.md §E5 linha 668; include/rdna4/mtp.cuh:365-372 | sem numero | somar `2 * max_ctx * NKV * kv_row_bytes` quando `--mtp` esta ligado; `info` usa `file_bytes + kv_cache_bytes + kOverheadBytes` (src/main.hip:918-923) | P2 | V | poucas linhas; evita hipMalloc failure numa config que o `info` aprova |
| 8 | `new-gpu-queue-hard-enforcement` | Enforcement estrutural da regra 1 da fila da GPU (nada impede um `bench` cru sem `flock`) | docs/gpu-queue.md linha 33 (o conserto descrito cobre apenas os 5 gates) | sem numero (12,07 GiB segurados sem flock; medianas 15-20% acima) | o wrapper exporta `GPU_LOCK_HELD=1`, mas um `./build/rdna4-infer bench` direto continua possivel | P2 | M | estrutural; a fila furada ja custou 15-20% de ruido nas medianas do lote |
| 9 | `delta-rule-rewrite` | `delta_rule_kernel`: 4 threads por linha com 32 elementos em registrador (ou 2 acumuladores + `float4`) | docs/medicoes-banda-e-gargalos.md §4.2 linha 430 | ate ~3 ms/token (8%); hoje 3,97 ms/token (11%) a 152 GB/s, 3,5-4,2x acima do piso de 20 us/camada | manter a ordem de soma documentada; gate `check-graph-gpu` (oraculo por no, tolerancia de reordenacao) + `check_golden_run.sh` + `compare_llama_greedy.sh` | P1 | M | ~3 ms/token (8%); kernel a 3,5-4,2x do piso de memoria |
| 10 | `gqa-kv-sharing` | Compartilhar K/V entre as 6 cabecas GQA (agora que a grade e larga) | docs/rocm-estudo.md §C linha 330 e §D.8 linha 365 | ate +18% a 64K (a atencao esta em ~1,56 TB/s de L2 por causa da redundancia 6x; teto ~2,4x na atencao) | grande, e o prototipo do M7 falhou por 8-12x mais lento (baixa confianca, [hipotese] nao testada com grade larga) | P1 | M | ate +18% a 64K; maior alavanca de trafego acima de 32K |
| 11 | `new-recheck-deferred-after-merges` | Re-verificar a lista de adiados da auditoria contra a arvore atual, porque merges posteriores ja fecharam parte dela | docs/auditoria-qualidade.md §7.3 linha 1108 | sem numero | SEM ISSO O BACKLOG CARREGA ITENS FEITOS: verifiquei nesta sessao que M2 (attn.cuh:405) e B1 (CMakeLists.txt:38,40,246) foram aplicados depois, enquanto E2 continua aberto; um backlog priorizado precisa dessa re-checagem item a item | P2 | V | 1 hora; ja provei que 2 dos adiados da auditoria estao feitos |
| 12 | `q3k-dp4a-linearity` | Eliminar a emulacao de `__vsubss4` no `q3_k` por linearidade do `dp4a` | docs/rocm-estudo.md §C linha 328 e §B.3 linha 258 | CONTRADICAO: §C diz ~0,15-0,2 ms (+0,5%); §B.3 diz 1,31 para ~0,7 ms (+1,7% de decode); qwen-kernels linha 408 diz +1,4-1,7% | pequeno-medio em vecdotq.cuh, precisa provar bit-exatidao; "nao implementado neste estudo por falta de orcamento" (item #2 do ranking) | P1 | M | +1,4-1,7% com esforco pequeno (resolver a contradicao de doc primeiro) |
| 13 | `new-mtp-kv-vram-accounting` | Contar no orcamento o KV proprio do MTP (duas caches de `max_ctx * NKV * kv_row_bytes`) | include/rdna4/mtp.cuh:365-372; src/main.hip:918-923 | sem numero | hoje `--mtp` carrega pesos do bloco 65 mais um KV inteiro fora de qualquer checagem de orcamento | P2 | V | mesmo item do mtp-kv-not-in-budget (documento+orcamento) |
| 14 | `rev-check-tuning-effective-values` | Imprimir no check-tuning os valores EFETIVOS dos consumidores (attn_split_wpb(2)/(16), attn_splits_for(4096), MtShape) | tests/check_tuning.cpp:60-82; include/rdna4/attn.cuh:560-582 | sem numero: fecha a classe 'gate verde com a constante fora de uso' | poucas linhas + regravar o golden | P1 | V | sem isso o gate responde 'a tabela e a medida', nao 'o motor usa a tabela' |
| 15 | `rev-attn-wide-branch-gate` | Gate de PPL para a ponta LARGA da regra de WPB: ctx 8192/16384, RD_ATTN_SPLITS 15 (WPB=8) contra 16 (WPB=16) | scripts/check_attn_split.sh; include/rdna4/attn.cuh:546-566 | 0 (gate de nao-regressao: a combinacao embarcada nao tem gate em texto real) | 2 corridas de PPL por par, ~1,5 min cada, numa janela livre | P1 | V | fecha a lacuna de evidencia do achado F5; eu tentei e fiquei na fila da GPU |
| 16 | `attn-head-dim-guard` | Exigir `head_dim <= WPB * 32` no `attn_launch_split_typed` | docs/auditoria-qualidade.md §M2 linha 349 | sem numero | robustez para variantes de qwen35 com `key_length > 256` (a validacao aceita ate 512) | P2 | V | 2 linhas; robustez para key_length > 256 |
| 17 | `m2-memset-partials-defensive` | `hipMemset(d_attn_partial_, 0, part_bytes)` no `init` por robustez | docs/auditoria-qualidade.md §M2 linha 350 | sem numero | 1 linha no init para que uma falha de logica apareca como zero e nao como lixo; recomendado e nao aplicado | P3 | M | 1 linha; falha de logica aparece como zero e nao como lixo |
| 18 | `h8-hipgetlasterror-semantics` | `hipGetLastError` pos-launch so ve erro de configuracao; erro "velho" vira falso positivo | docs/auditoria-qualidade.md §H8 linha 245 e conserto na linha 269 | sem numero | 1 linha por launcher (`(void)hipPeekAtLastError()` antes do `<<<>>>`) + `hipDeviceSynchronize` checado onde o resultado e consumido; achado IMPORTANTE nao corrigido e nao listado em §7.3 | P2 | M | 1 linha por launcher; achado IMPORTANTE da auditoria nao listado como adiado |
| 19 | `iq3s-read-ceiling` | `iq3_s` no teto de leitura: LDS para a LUT `iq3s_grid` e/ou a formulacao fp32 estilo Vulkan | docs/autotuning-gfx1201.md §5.2 linha 265 | estimativa 8,08 para ~6,5 ms = +5% no decode (hoje ~443 GB/s contra teto medido de 619 GB/s) | caminho com evidencia no repo (o Vulkan/RADV poe a LUT em LDS); a variante fp32 tem 4x mais bytes de ativacao | P1 | M | estimado +5% (LUT em LDS e/ou formulacao fp32 estilo Vulkan), bit-exato |
| 20 | `shape-indexed-tuning-table` | Tabela de shape indexada por (tipo, blocos-por-linha) para atacar a classe `q8_0` 48x160 | docs/autotuning-gfx1201.md §4.1 linha 247 e §5.1 linha 261 | estimativa +2% a +3,5% no decode (recuperando metade a tudo daquele 1,0 ms/token); a classe roda a 25 GB/s e custa 1,00 ms/token = 4% de todo o matvec | bit-exato por construcao (`ROWS` e livre; `WPR` exigiria validacao); a tabela atual e indexada so por tipo e nao consegue expressar "esta forma precisa de outra configuracao" | P1 | M | estimado +2 a +3,5%; ataca a classe q8_0 48x160 a 25 GB/s |
| 21 | `kv-q5-0-q4-1-implement` | KV `q5_0` (K) e `q4_1` (V) nao existem no motor | docs/journal-noite.md:26-28; docs/noite-regras.md:16-23 | conta preliminar 21 504 B/token => 2,63 GiB em 131K (contra 2,25 do q4_0 e 4,25 do q8_0) | "e hipotese a implementar e medir, nao resultado"; exige novos `KvType` e encoders byte-identicos ao ggml em kv.h | P1 | M | alvo declarado da noite: sem ele 131K fica em q4_0/q8_0 |
| 22 | `new-peek-last-error-sweep` | Varredura de `(void)hipPeekAtLastError()` nos 32 launchers de producao | docs/auditoria-qualidade.md §H8 linha 269 | sem numero | 1 linha por launcher; o conserto esta sugerido na auditoria mas nao aparece nem na lista de corrigidos nem na de adiados | P2 | M | varredura mecanica nos 32 launchers; H8 e o mesmo conserto |
| 23 | `new-serve-auth-token` | Adicionar autenticacao (ou recusar `--host 0.0.0.0` sem token) | src/server/serve.hip; docs/servidor-openai.md:95 | sem numero | hoje um flag expoe um endpoint sem autenticacao a LAN; o README avisa, mas nao ha barreira tecnica | P2 | M | poucas linhas; hoje --host expoe endpoint sem autenticacao |
| 24 | `hipgraph-matvec-sequence` | HIP graph da sequencia de matvec | docs/rocm-estudo.md §C linha 327 e §D.6 linha 356 | 1,06x = 1,38 ms/token; >=1,4 ms (+4%) medido no matvec | medio; incompativel com o `hipMemcpy` da posicao hoje; nao e o fator que explica a distancia para o llama.cpp | P2 | M | medido 1,06x no matvec (1,4 ms); o prototipo ja existe |
| 25 | `e6-duplicated-token-batch` | ~150 linhas duplicadas entre o caminho por token e o em batch em graph.cuh | docs/auditoria-qualidade.md §E6 linha 689 | sem numero | extrair o corpo por token para funcoes com stride parametrizado (adiado: refatoracao de estilo sem ganho de correcao) | P2 | M | ~150 linhas duplicadas; a classe de bug 'conserto um caminho e esqueço o outro' |
| 26 | `new-tuning-gate-coverage` | Verificar a cobertura do gate `check-tuning` (22 linhas / 22 chaves obrigatorias) contra todas as constantes de atencao | docs/autotuning-gfx1201.md §6 linha 419 e PLAN.md:648-650 | sem numero | o gate existe e e CPU puro; falta saber se `kAttnSplitMin`/`kAttnSplitWpbWide`/`kMaxBatch` estao todos cobertos | P3 | V | verificacao; eu ja respondi: o gate cobre a tabela e nao a fiacao (F1/F10) |
| 27 | `m10-widening-47-sites` | Converter na primeira multiplicacao os 47 sitios de alargamento implicito | docs/auditoria-qualidade.md §7.3 linha 1120 | sem numero (186 avisos de clang-tidy em 47 linhas) | toca as mesmas linhas que a frente de autotuning editava (adiado por conflito de arquivos); latente, nenhuma entrada hoje dispara | P2 | V | 47 sitios; latente hoje, mas e a classe que a frente KV vai tocar |
| 28 | `new-ctest-wiring` | Ligar CTest para os 25 binarios de teste | docs/auditoria-qualidade.md §E11 linha 823 | sem numero | `enable_testing()` mais `add_test()`; confirmado vazio com grep de `enable_testing` e `add_test` no CMakeLists; hoje cada teste tem de ser chamado a mao | P2 | V | enable_testing()+add_test(); hoje cada teste e chamado a mao |
| 29 | `rev-kv-q8-0-vs-q4-0-ab` | Re-medir q8_0 x q4_0 a 64K em A/B intercalado na MESMA janela | docs/medicoes-banda-e-gargalos.md:485-490 contra docs/quants-precisao.md:249-254 e README.md:202 | delta publicado de +0,5% a +1,8%; hoje menor que a variabilidade entre sessoes (2-3%) | 2 corridas de bench no mesmo lock | P2 | V | hoje ha TRES numeros para a mesma configuracao (19,3 / 18,28 / 18,36) e zero A/B |
| 30 | `rev-readme-phase-table-partition` | Corrigir a particao da tabela 'Where a token goes' (head dentro de (a) E de (f); sampler host somado a um total de dispositivo) | README.md:240-248; docs/medicoes-banda-e-gargalos.md:137-145 | fecha a soma de 37,87 contra o total declarado de 36,0 (5,2% de overcount) | editar 2 tabelas | P2 | V | a tabela fina do mesmo doc soma 35,98: o erro e so na agregacao |
| 31 | `rev-readme-bandwidth-after-fix` | Re-derivar as linhas de banda do README depois do conserto do bench | README.md:199,205-206,298-299 | 352 -> ~325 GB/s (56% -> 51% da roofline de 633 GB/s); '402-421 measured' e estimativa | depende do conserto de bytes/token no bench | P2 | V | numero publicado herda o vies de 8,2% do achado F4 |
| 32 | `e5-comments-that-lie` | 17 comentarios/doc-strings que o codigo ja nao cumpre | docs/auditoria-qualidade.md §E5 linha 662; include/rdna4/device.h:33 e :38; include/rdna4/graph.cuh:15-16; src/main.hip:194-195 | sem numero | corrigir comentarios (achado IMPORTANTE nao corrigido): device.h:33 diz "17 full-attention layers", graph.cuh:15 diz "f32 KV (quantized cache types are a later step)", main.hip:194 diz "this engine has no batched prefill" | P3 | V | 17 comentarios; barato e reduz custo de leitura de todos os proximos agentes |
| 33 | `e3-comments-unsplit-kernel` | Comentario e doc descrevem um kernel sem split que nao existe mais (a ordem de soma nao muda com WPB) | docs/auditoria-qualidade.md §E3 linha 629; include/rdna4/attn.cuh:88-92 | sem numero | corrigir 2 comentarios junto com E2 (adiado junto com E2); o texto errado segue vivo em attn.cuh:88-92 | P3 | V | 2 comentarios, junto com E2 |
| 34 | `new-reconcile-q8-0-64k` | Corrigir a contradicao interna de §3.3 contra §4.4 do mesmo documento sobre o q8_0 a 64K | docs/medicoes-banda-e-gargalos.md §3.3 linha 386 contra §4.4 linha 485 | §3.3 diz "nao medido ... teste #1 da proxima sessao"; §4.4 mede 19,87 ms | quem ler §3.3 isolado planeja um experimento ja feito | P3 | V | 1 paragrafo; §3.3 e §4.4 do mesmo doc se contradizem |
| 35 | `new-repin-stale-doc-pointers` | Varrer e re-pinçar os ponteiros doc-para-codigo que a reescrita do README invalidou | README.md (nao tem mais as linhas 94/132-138 citadas); docs/kv-memoria-desenho.md §7.4 linha 462 | sem numero | a auditoria cita README.md:132-134, :94, :138 e graph.cuh:315; na arvore atual README.md:132 e "Correction of record" e o `atoi` esta em graph.cuh:332 | P3 | M | varrer ponteiros doc->codigo invalidados pela reescrita do README |
| 36 | `new-wikitext-fetch-script` | Criar o script que baixa o corpus wikitext-2 | scripts/check_attn_split.sh:37 cita `scripts/get-wikitext-2.sh` inexistente | sem numero | sem ele, `check_attn_split.sh` e `compare_ppl.sh` dependem de `reference/`, que e gitignored e nao viaja no clone | P3 | V | script de 10 linhas; hoje 2 gates dependem de reference/ (gitignored) |
| 37 | `new-audit-static-only-caveat` | O relatorio de auditoria e estatico: os numeros de GPU dos docs nao foram re-medidos | docs/auditoria-qualidade.md §5 linha 1001 | sem numero | as contradicoes apontadas sao internas aos documentos e ao codigo; qualquer item de desempenho deste backlog precisa de medicao nova antes de ser aceito | P3 | M | registro: todo item de desempenho deste backlog precisa de medicao nova |
| 38 | `rev-delete-required-bytes` | Apagar as duas sobrecargas mortas de required_bytes (a de um tipo contem a formula pre-M4) | include/rdna4/device.h:76-87 | sem numero (armadilha: nome mais natural de chamar, semantica que o M4 provou errada) | 12 linhas | P2 | V | o README chama kv_cache_bytes de 'one source of truth'; o arquivo tem duas |
| 39 | `rev-kv-fill-q4-0-block-scale` | kv_fill_kernel Q4_0: reduzir o maximo assinado por warp e publicar b_scale de uma lane so | include/rdna4/kv.h:280-298 | sem numero: hoje 16 lanes correm no b_scale do mesmo bloco e o resultado nao e deterministico | poucas linhas no kernel de hook | P2 | V | o Q8_0 logo acima faz certo; a assimetria e o bug |
| 40 | `rev-hardening-git-archive-rev` | check_hardening.sh: aceitar commit-ish e extrair com git archive; passar o rev no check_all | scripts/check_hardening.sh:22-23,62-89; scripts/check_all.sh:53 | sem numero: mantem a coluna 'antes' reproduzivel (o worktree pre-correcao nao existe mais) | ~15 linhas de shell | P2 | V | eu reproduzi as 5 linhas da §7.1 assim, em ~10 s |
| 41 | `e8-dead-code` | Codigo morto comprovado: `scale_launch`, `required_bytes()`, `matvec_default_ilp`, `__vcmpeq4`, `get_int_b1`, `attn_splits_last()`, bos/eos/pad_token_id | docs/auditoria-qualidade.md §E8 linha 748; include/rdna4/device.h:76,84; include/rdna4/graph.cuh:341 | sem numero | marcar `// bench-only, sem uso em src/` e resolver 15 simbolos publicos sem chamador (adiado: nao apagar sem confirmar intencao, sao evidencia de A/B); `required_bytes` continua com 0 chamadores nesta arvore | P3 | V | 15 simbolos publicos sem chamador; nao apagar sem confirmar intencao |
| 42 | `e4-readme-spec-two-numbers` | README/SPEC com dois numeros para o mesmo decode a 4K e "32 warps/CTA" | docs/auditoria-qualidade.md §E4 linha 636; SPEC.md:74 | "1.1-5.8x"; "23.7 tok/s at 4K" contra "26.8 tok/s" | trocar 2 trechos (README ja reescrito nesta arvore; SPEC.md:74 ainda diz "32 warps por CTA") | P3 | V | 2 trechos; SPEC.md:74 ainda diz '32 warps por CTA' |
| 43 | `new-spec-section3-refresh` | Atualizar a tabela normativa do SPEC §3 com os numeros finais medidos | SPEC.md:58-101 (coluna de prefill antiga e "32 warps") | sem numero | o SPEC e a referencia citada pelo PLAN; hoje contradiz README e medicoes-m7 | P2 | M | atualizar a tabela normativa do SPEC com os numeros finais |
| 44 | `new-readme-numbers-provenance` | Explicitar a proveniencia dos numeros da tabela final (8-9% melhores que os registros do M7/M8 pela janela, contra +1,2% do A/B controlado) | README.md:224-227 | 4K: 26,8 para 29,3; A/B controlado mediu +1,2% | regra de leitura de numero: separar ganho de mudanca de estado da maquina; hoje e uma nota de rodape | P3 | M | nota de proveniencia; separa ganho de mudanca de estado da maquina |
| 45 | `rev-profiler-robustness` | Profiler: macro avalia o argumento 2x, eventos sem hipEventDestroy, on=false silencioso | include/rdna4/phase_prof.cuh:82-121,161-164 | sem numero | poucas linhas | P3 | V | o profiler E inerte (verificado em todos os sitios); sao arestas |
| 46 | `rev-rocm-estudo-f1-nonregression-note` | Corrigir o argumento de nao-regressao '>=8 splits roda o mesmo caminho de antes' | docs/rocm-estudo.md:486-492 contra docs/autotuning-gfx1201.md:74 | sem numero | 1 paragrafo | P3 | V | depois da regra de duas pontas esse argumento deixou de valer para >=16 splits |
| 47 | `rev-gguf-remaining-guard-nonseekable` | Registrar/checar que a metade 'cabe no resto do arquivo' do M8 fica inativa se ftell falhar | src/backend/gguf.cpp:24,61-67,163-171 | sem numero | 1 comentario ou 1 checagem | P3 | V | com arquivo regular os dois freios valem; o teto absoluto nunca depende disso |
| 48 | `qwen-wmma-mmq-prefill` | WMMA int8 + MMQ no prefill (rota (a): manter quantizado, desquantizar para int8 dentro do kernel, staging em LDS, layout MMA por tipo) | docs/qwen-kernels.md §5 item 1 linha 404 | 70 para 300-440 tok/s de prefill: o maior ganho absoluto do repo | milestone (familia de kernels nova para 14 tipos + dispatch); bit-exatidao nao se aplica (muda a ordem de soma) => tolerancia + `compare_ppl.sh` + `bench --prefill` | P1 | M | maior ganho absoluto do repo (70 -> 300-440 tok/s), mas e item de milestone |
| 49 | `fuse-small-kernels` | Fundir os kernels pequenos (cadeia escalar do GDN, `rms_norm`+`quantize`, `kv_write`) | docs/rocm-estudo.md §C linha 326 e §E.2 linha 405 | +3-10% [estimativa nao medida: ~1900 lancamentos x 3,5 us]; docs/medicoes-banda-e-gargalos.md §4.3 linha 469 mede ~1,1 ms/token (3%) | kernel novo + gate PPL; fusao bit-exata mas exige mexer em graph.cuh (grande) | P1 | M |  |
| 50 | `prefill-mmq-wmma` | Prefill em batch com MMQ/WMMA int8 e staging em LDS | docs/rocm-estudo.md §C linha 329 | 28,8 para 63 tok/s (matvec batch, M6) e ate 300-440 com MMQ | milestone: familia de kernels nova para 14 tipos + dispatch; o matvec so cobre batch <= 8 (PLAN.md:165) | P1 | M |  |
| 51 | `vulkan-fuse-scalar-norm-chains` | Fundir as cadeias escalares e de norma (8 fusoes nomeadas, ~640 lancamentos) | docs/vulkan-vs-hip.md §4.1 linha 515 | ~640 x 3,5 us ~= 2,2 ms/token = +6,5% de decode, mais ~0,5 ms do trabalho real da cadeia escalar do GDN | bit-exato; gate `check-graph-gpu` + `check_golden_run.sh` + `check-batch-gpu`; risco baixo, cada fusao independente; a unica delicada e `rms_norm`+`quantize` (manter a ordem da reducao em arvore) | P1 | M |  |
| 52 | `vulkan-iq-lut-in-lds` | LUT dos IQ em LDS (em vez de `__device__` global com 8 `global_load_b32` por `vec_dot`) | docs/vulkan-vs-hip.md §4.2 linha 541 | alvo > 5% em `iq3_s` (30,9% dos bytes) | 2-8 KB de LDS/CTA reduzem a ocupacao; medir antes de generalizar para todos os tipos (a LUT do `iq1_s` e 4 KB) | P1 | M |  |
| 53 | `vulkan-gqa-per-workgroup-attn` | Atencao estilo Vulkan: grupo GQA por workgroup e splits largos | docs/vulkan-vs-hip.md §4.4 linha 575 | alvo 64K f16 de 1,19 ms/camada para < 0,7 ms (o gap relativo e ~58% a 32K) | medio-alto: e o kernel onde o M7 ja levou uma surra; nao e bit-exato, gate e PPL em texto real | P1 | M |  |
| 54 | `vulkan-gemm-tiled-prefill` | GEMM tiled para prefill (pesos e ativacao em LDS, cache em registradores, tile BMxBN) | docs/vulkan-vs-hip.md §4.5 linha 598 | hoje 7,37 s e 29,18 s; alvo incremental > 120 tok/s antes de qualquer fusao de precisao; teto 300-440 | esforco de milestone; RELAXA a bit-exatidao (decidir explicitamente antes de comecar); sem receita pronta para `iq3_s`/`iq4_xs`/`iq3_xxs`, que sao 68% dos bytes | P1 | M |  |
| 55 | `kv-policy-by-context-cli` | Politica de KV por contexto no CLI/servidor (escolher sozinho `f16` ate 48K e `q8_0` acima) | docs/medicoes-banda-e-gargalos.md §4.5 linha 503 | sem numero (~6,7x de ganho imediato trocando o tipo de KV a 64K, onde o f16 da 8x de queda) | a decisao precisa do `hipMemGetInfo` antes do `Graph::init`, que ja e chamado em `cmd_bench`/`cmd_run` | P1 | M |  |
| 56 | `qwen-vec-dot-alu-reduction` | Reduzir ALU de indice/seletor nos `vec_dot` dos 3 i-quants dominantes (73% do tempo de matvec) | docs/qwen-kernels.md §5 item 2 linha 405 | -2 ms => +5-6% de decode (-10% de instrucoes em 73% do matvec, que e 26,4-27,7 ms) | medio-grande em vecdotq.cuh; confianca media (a contagem de ISA e medida, o ganho nao foi implementado) | P1 | M |  |
| 57 | `kv-store-rows-launch` | Escrever os 4 heads de K e de V num unico lancamento (`kv_store_rows_launch`) | docs/kv-memoria-desenho.md §5 proposta 2 linha 399 | -120 lancamentos/token ~= 0,42 ms (1,2%) [estimativa: 120 x 3,5 us] | helper novo em kv.h + graph.cuh:701-709; a fusao preserva a aritmetica por linha (bit-exato) | P1 | M |  |
| 58 | `mtp-batched-verify` | Verify batelado do MTP (atencao multi-query com mascara causal + forward batelado do tronco) | docs/mtp.md §7 linha 265 e §4 linha 189 | extrapolacao: uma rodada D=4 custaria ~36 ms + ~23 ms para ~4,0 tokens => 14,7 ms/token contra 36,1 agora = ~2,5x; o M8 mediu 1,3-1,5x no melhor caso | toca attn.cuh/gdn.cuh; a parte dificil e o rollback do estado recorrente | P1 | M |  |
| 59 | `prefill-restructure-m6-step2` | Reestruturar para prefill batched completo ou kernel tiled estilo MMQ | PLAN.md:553-563 (M6 passo 2a/2b/2c/2d); docs/medicoes-m6.md linha 92 | 2a: ~63 tok/s; 2b: teto ~145 tok/s; 2c: ~300-440 tok/s; 2d: tiling da atencao | "a decisao de gastar isso e do usuario, com estes numeros na mao" (2c perde bit-exatidao e precisa de rodada propria de validacao) | P1 | M |  |
| 60 | `paralelos-mtp-batch-verification` | Verificacao em batch para o MTP (~2,5x potencial) | docs/agentes-paralelos.md linha 80 | ~2,5x potencial | e o mesmo trabalho do prefill em batch (M6 passo 2), entao as duas coisas devem ser feitas juntas | P1 | M |  |
| 61 | `paralelos-prefill-usability-gap` | O prefill em batch continua sendo a maior lacuna de usabilidade para prompt longo | docs/agentes-paralelos.md linha 85 | sem numero | parcialmente fechado pelo M8 (70 tok/s); a lacuna que resta e o laco interno do `vec_dot` | P1 | M |  |
| 62 | `plan-mmq-cuh-in-m5` | Depois (M5): `mmq.cuh` + `mmq-config-rdna4.cuh` para prefill (batch > 8) | PLAN.md:165 | sem numero (o matvec so cobre batch <= 8, igual ao `MMVQ_MAX_BATCH_SIZE`) | trabalho de milestone separado | P1 | M |  |
| 63 | `hipgraph-full-sequence-measure` | Medir o grafo HIP sobre a sequencia inteira (o teste que falta) | docs/rocm-estudo.md §B.5 linha 315; docs/qwen-kernels.md linha 385 | teto estimado de ~1900 lancamentos ~= 7 ms (nao medido) contra 1,38 ms (4%) de fato medidos | barato, e e ele que decide entre "fundir kernels" (grande) e "capturar um grafo por token" (medio) | P2 | M |  |
| 64 | `argmax-on-device` | Argmax no device (caminho greedy) | docs/rocm-estudo.md §C linha 331 | ~+0,3-0,5 ms (1%) | pequeno-medio em graph.cuh e main.hip; em empate de logits o desempate tem de ser o mesmo do host | P2 | M |  |
| 65 | `bench-discount-token-embd-mtp` | `bench` descontar `token_embd` e o bloco MTP do byte count | docs/rocm-estudo.md §C linha 332 e §A.2.6 linha 145 | 0 em tok/s; corrige o numero de banda reportado em 8% | trivial em main.hip; AINDA ABERTO: src/main.hip:373 usa `loader.total_bytes()` | P2 | M |  |
| 66 | `rocm-lut-sign-selector` | LUT de seletor de sinais de 16 entradas trocaria ~5 ALU por 1 load | docs/rocm-estudo.md §B.2 linha 245 | sem numero (~57 das 171 instrucoes do `iq3_s` sao aritmetica de indice/seletor) | mexer no `vec_dot` do `iq3_s`, que e 33% do trafego; bit-exatidao a provar | P2 | M |  |
| 67 | `oracle-dump-recapture` | Recapturar o dump do oraculo antes de usar `check-graph-gpu` como aceite | docs/rocm-estudo.md §F.1 linha 534 | sem numero (39 nos falhando com magnitudes 4-16 = outro resultado, nao arredondamento) | rodar `scripts/capture_oracle.sh`; a hipotese do doc e que o dump nao corresponde ao estado atual do motor | P2 | M |  |
| 68 | `vulkan-argmax-device` | Amostragem greedy no device (`ggml_argmax` como no do grafo) | docs/vulkan-vs-hip.md §4.3 linha 559 | impacto ~1-1,5% (0,5 ms de copia + sync + softmax host) | pequeno; gate `check-sampler` continua valendo para temperatura; greedy vira golden | P2 | M |  |
| 69 | `vulkan-kv-dequant-transposed-pass` | KV quantizado via passe de dequant transposto para scratch f16 | docs/vulkan-vs-hip.md §4.6 linha 622 | alvo `q4_0` >= `f16` no mesmo contexto, o que compraria ~0,88 GiB a 64K e ~1,8 GiB a 131K | so paga quando o cache e grande (criterio deles: `neq1 >= 64`); medir o ponto de virada antes de ligar por default | P2 | M |  |
| 70 | `vulkan-observability-knobs` | `RD_FUSE=0`, `RD_MATVEC_F32=1` (variante FMA fp32 do `iq3_s`) e instrumentacao de tempo por no | docs/vulkan-vs-hip.md §4.7 linha 641 | impacto indireto: permite medir o ganho de cada fusao e responder se `dp4a`+`perm` ganha do FMA fp32 | barato; a ferramenta `check-matvec-gpu --bench-ab` ja compara variantes intercaladas | P2 | M |  |
| 71 | `attn-max-splits-24` | `kAttnMaxSplits` 16 para 24 | docs/autotuning-gfx1201.md §3.2 linha 184 e §5.3 linha 270 | +2,8% (24x8) e +8,7% (24x16) em q4_0/131K, mas 0,921x/0,974x/0,991x em f16 16K/64K/131K = regressao | o otimo do numero de splits depende do tipo de KV e uma constante unica nao expressa os dois (rejeitado como constante, virou candidato) | P2 | M |  |
| 72 | `attn-few-splits-wide-cta-4k` | "Poucos splits + CTA larga" a 4K/16K (4x16) | docs/autotuning-gfx1201.md §3.2 linha 189 e §5.3 linha 270 | 1,046x no kernel; end-to-end 0,18% a 4K e ~0,5% a 16K (abaixo ou na fronteira do ruido de decode de +-1-2%) | exigiria mexer em `kAttnSplitMin` (piora 16K/64K) ou tornar a regra dependente de `keys` (fora do escopo da tarefa: e politica, nao tabela) | P2 | M |  |
| 73 | `attn-policy-keys-dependent` | Politica de atencao dependente de `keys` (poucos splits + 16 warps a 4K/16K; 24 splits com 16 warps a 131K q4_0) | docs/autotuning-gfx1201.md §5.3 linha 270 | estimativa +0,2% a 4K, +0,5% a 16K, +2,4% a 131K q4_0 (o ganho no kernel esta medido: +4,6% e +8,7%) | regra nova em `attn_split_wpb()`/`attn_splits_for()`; [estimativa, nao medido]; vale a pena se o alvo for contexto longo | P2 | M |  |
| 74 | `q4-0-attn-vectorized-load` | Caminho de leitura vetorizado/LDS para o `q4_0` na atencao | docs/medicoes-banda-e-gargalos.md §3.3 linha 382 e §4.4 linha 491 | a 64K a atencao e 38% do token (20,97 de 55,02 ms) com 7,25 GB emitidos a 346 GB/s, contra 13,7 GB a 690 GB/s do q8_0 | o gargalo e o desempacotamento por elemento (`kv_load8<Q4_0>` desempacota nibble a nibble em ALU); dono e quem mexe em kv.h | P2 | M |  |
| 75 | `vram-preflight-test-05gib` | Gate que compare `vram in use` contra o total e falhe se sobrar < 0,5 GiB | docs/medicoes-banda-e-gargalos.md §4.5 linha 508 | o ponto de falha e medido: 48K f16 = 1,30 GiB livres OK, 64K f16 = 0,30 GiB livres e transborda | teste novo, junto com `check-kvctx-gpu` | P2 | M |  |
| 76 | `rms-norm-5120-real-cost` | `rms_norm(1x5120)` custa 10,90 us por lancamento (8,7 us acima do piso de despacho) | docs/medicoes-banda-e-gargalos.md §4.3 linha 454 | 129 lancamentos x 8,7 us = 1,1 ms/token | hoje e 1 CTA de 256 threads numa CU com 8 `__syncthreads` na reducao; fundir com o `quantize` resolve (o valor normalizado ja esta em registrador) | P2 | M |  |
| 77 | `kv-f16-gtt-boundary` | Onde exatamente comeca o transbordo para GTT (KV f16 a 32K/48K) | docs/medicoes-banda-e-gargalos.md §6.3 linha 540 | sem numero (ficaram fora do orcamento de GPU da sessao) | medir com `gtt_used` do sysfs; o limite conhecido e "48K cabe, 64K transborda" | P2 | M |  |
| 78 | `qwen-precompute-rop-cos-sin` | Pre-computar `cos/sin` por posicao e passar `pos` por valor ao kernel | docs/qwen-kernels.md §2.2 linha 96 | sem numero (731 instrucoes por lancamento porque calcula `powf` + `cosf` + `sinf` por elemento; a RoPE e a razao do `hipMemcpy` de 4 bytes) | [hipotese]; elimina os dois (a copia sincrona e o calculo redundante por elemento) | P2 | M |  |
| 79 | `qwen-delta-rule-grid-widen` | `delta_rule`: dividir a dimensao `j` para encher as 64 CUs (hoje 48 CTAs) e vetorizar as linhas com `float4` | docs/qwen-kernels.md §5 item 7 linha 410 | ~0,9 ms de trafego (2,5%) mais as 16 CUs ociosas; ganho real desconhecido | pequeno-medio; mudar o numero de threads nao muda a aritmetica (bit-exato) | P2 | M |  |
| 80 | `qwen-kv-row-bytes-spec-align` | Fechar a brecha silenciosa de `kv_row_bytes` e alinhar `SPEC.md:21` com a whitelist real | docs/qwen-kernels.md §5 item 10 linha 413 | 0 em tok/s; remove uma classe de falha silenciosa | trivial em kv.h e SPEC.md | P2 | M |  |
| 81 | `qwen-bench-share-correction` | `bench` descontar `token_embd` + MTP e as shares da §3 do inventario | docs/qwen-kernels.md §5 item 9 linha 412 | 0 em tok/s; corrige 8% do numero de banda reportado, provado por aritmetica em quants-inventario §3.3 | trivial em main.hip e docs/medicoes-m6.md; AINDA ABERTO (src/main.hip:373) | P2 | M |  |
| 82 | `qwen-launch-budget-missing-test` | A medicao que falta e o grafo HIP sobre a sequencia inteira | docs/qwen-kernels.md §4 linha 385 | teto ~7 ms (20%) [estimativa] contra 1,38 ms (4%) de fato medidos | e ela que decide entre fundir kernels (grande) e capturar um grafo por token (medio); fazer o grafo primeiro e barato e transforma o teto em numero | P2 | M |  |
| 83 | `kv-fuse-split-merge` | Fundir o merge do split no ultimo split | docs/kv-memoria-desenho.md §5 proposta 3 linha 400 | 16 lancamentos mais 1 ponto de sync/token ~= 0,1-0,2 ms; tira 6,34 MB de vaivem a 4K | media: muda a ordem da soma, deixa de ser bit-exato (mesmo regime do M7) | P2 | M |  |
| 84 | `kv-lds-staging-line` | Staging em LDS da linha KV dentro do CTA (uma linha de 512 B por vez, 6 cabecas consumindo) | docs/kv-memoria-desenho.md §5 proposta 8 linha 405 | mesmo teto da proposta 1 (GQA sharing) com menos risco de registrador | baixa-media: nao ha `global_load_lds` em gfx1201, o staging tem de passar por registrador, e o `__syncthreads` por chave pode custar mais que o ganho | P2 | M |  |
| 85 | `kv-reduce-partials-traffic` | Reduzir o vaivem dos parciais do split (3,02 MiB a 4K, 6,05 MiB a 64K por sentido) | docs/kv-memoria-desenho.md §3.3 linha 233 | ~0,06 ms [estimativa: 16 lancamentos x 3,5 us] mais a serializacao da CTA que espera o merge | fundir o merge no kernel (ultima CTA) ou reduzir splits | P2 | M |  |
| 86 | `mtp-long-context` | MTP em contexto longo (so medido a 4K) | docs/mtp.md §7 linha 268 | a cache KV do bloco e f16 e custa 8 MiB a 4K (2 MB por 1K de contexto), entao a 64K sao 128 MiB | medir; CONTRADICAO: include/rdna4/mtp.cuh:365-372 aloca DUAS caches (`d_ck_` e `d_cv_`), 4 KiB/token => 16 MiB a 4K e 256 MiB a 64K (auditoria §E5 linha 682) | P2 | M |  |
| 87 | `spec-rollback-checkpoints` | Rollback do estado recorrente via checkpoints | docs/medicoes-m8.md linha 85 | 1,3-1,5x no melhor caso; a variante simples (aceitar so a rodada inteira) mede 1,0-1,1x, ou seja, nao paga | snapshot de `d_state_`/`d_convst_` (157 MB, ~0,5 ms) + captura dos tensores de entrada do GDN por token/camada (~8 MB por rodada) + replay da recorrencia so do prefixo aceito (~0,8 ms/token) | P2 | M |  |
| 88 | `cli-multiturn-flag` | Nao ha flag de conversa multi-turno no CLI | docs/medicoes-m5.md §5.4 linha 194; README.md:376-377 | sem numero (`chat_render` suporta multi-turno e e testado) | expor um flag de arquivo de conversa | P2 | M |  |
| 89 | `serve-no-auth` | O servidor nao tem autenticacao | docs/servidor-openai.md linha 95 | sem numero | `--host 0.0.0.0` expoe um endpoint sem autenticacao a rede | P2 | M |  |
| 90 | `night-131k-measure` | 131K ainda nao foi medido nesta rodada | docs/journal-noite.md:24 | no baseline, 131K com KV q4_0 roda a 14,0 tok/s com 13,88 GiB em uso; com f16 nao cabe (2-8 tok/s) | medir; e o alvo declarado da rodada noturna | P2 | M |  |
| 91 | `build-arch-autodetect` | O CMake injeta tambem o arch que detecta na maquina | docs/build-repro.md §6.3 linha 447 | sem numero (em maquina sem GPU ou com outra AMD o build compila para outra arquitetura e quebra: `needs target feature gfx12-insts`) | fixar `-DCMAKE_HIP_ARCHITECTURES=gfx1201` explicitamente fora da maquina de referencia | P2 | M |  |
| 92 | `plan-fp16-wrapper-hardening` | Trocar o tipo dos campos fp16 por um wrapper `struct fp16 { uint16_t bits; operator float() const; }` | PLAN.md:78 | sem numero | "hardening pendente (proximo passo pequeno)": pegaria a classe de bug que apareceu 3 vezes (block_q5_K com qh/qs trocados, `d` de q3_K/q6_K/q8_0 lido como inteiro) | P2 | M |  |
| 93 | `plan-m4-open-test-gaps` | Ainda em aberto (consciente) do M4: o teste cross-engine depende de o EOG ser o mesmo dos dois lados; falta um teste que amarre "prompt renderizado para ids do llama.cpp"; o laco de chat do CLI e single-turn | PLAN.md:390 | sem numero | lacunas de teste declaradas; nenhum caso forca um EOG no meio da geracao | P2 | M |  |
| 94 | `iq1s-iq2-truncation-fix` | Correcao de truncamento em `iq1_s`/`iq2_*` | docs/quants-precisao.md §7.3 linha 283 e §2.1 linha 93 | o ganho maximo medido e 4,4e-04 de erro relativo em 0,3% do token (o `iq1_s` e 4000x pior que o pior dos outros) | nao implementado (mudanca em vecdotq.cuh, dono e a frente de kernels); um `default: __builtin_trap()`-style fix derrubaria o numero ao nivel dos outros sem tocar no tamanho do arquivo | P2 | M |  |
| 95 | `kv-q4-0-vs-f16-ppl` | KV `q4_0` contra `f16` na *qualidade* a contexto longo | docs/quants-precisao.md §7.4 linha 286 | sem numero | so o `q8_0` foi comparado em velocidade; a PPL a 64K com cada tipo de KV nao foi medida (custo de GPU) | P2 | M |  |
| 96 | `q3k-bandwidth-disagreement` | A discordancia do `q3_k` (-12%: 271 GB/s aqui contra 307 do estudo) | docs/quants-precisao.md §5 nota 2 linha 221 | 271 contra 307 GB/s | "vale re-medir com A/B intercalado antes de atribuir causa" (a sessao deu os 582 GB/s do `q5_k` corretos) | P2 | M |  |
| 97 | `exploracao-open-questions` | Sete perguntas abertas da exploracao da arquitetura (mapeamento tensor-para-op nas 48 lineares, tamanho real do estado GDN, `ssm_a`/`ssm_dt` em F32, divergencia de EOS entre config.json e GGUF, sensibilidade por tensor via imatrix, KV q4_0 nas full layers, bloco 64 como draft) | docs/exploracao-qwen38.md §2 linhas 21-27 | sem numero | o doc diz explicitamente que nao e compromisso: e o mapa do que falta entender; a #4 (EOS 248044 contra 248046) ainda e uma divergencia nao resolvida | P2 | M |  |
| 98 | `exploracao-e4-long-positions` | E4: validar MRoPE alem de 32K (64K/128K ficaram para o M5 com KV quantizado) | docs/exploracao-qwen38.md §3 linha 34 | sem numero | pendente de medicao de qualidade em posicoes longas | P2 | M |  |
| 99 | `hw-global-load-block` | Usar `GLOBAL_LOAD_BLOCK`/`GLOBAL_STORE_BLOCK` (ate 32 VGPRs por instrucao, novo em RDNA4) | docs/rdna4-gfx1201-hardware-brief.md §7.3 linha 70 | sem numero | 0 usos na arvore (grep por `global_load_block` e `global_store_block` em include/ e src/ volta vazio); mexeria no matvec e na atencao | P2 | M |  |
| 100 | `hw-packed-half-1-5x` | `v_pk_fma_f16` e `v_dot2_f32_f16` dao 3x o trabalho de FMA por instrucao a 1,5x o rate: usar em algum kernel | docs/rdna4-gfx1201-hardware-brief.md §2 linha 27 e §7.1 linha 68 | 1,5x o rate do FMA fp32 (medido com `SHADER_CYCLES`) | 0 usos na arvore; serve so para pesos fp16 (nao para os nossos int8); a atencao com KV f16 e o candidato | P2 | M |  |
| 101 | `hw-dpm-high-before-measuring` | Antes de medir: `echo high > power_dpm_force_performance_level` e conferir ausencia de nivel `S:` | docs/rdna4-gfx1201-hardware-brief.md §7.5 linha 72; docs/noite-regras.md §3.2 linha 70 | sem numero (a placa cai a SCLK 9-16 MHz entre kernels) | `scripts/gpu-lock.sh` so faz `flock`, nao toca no DPM; o warm-up de >=300 ms e o paliativo atual | P2 | M |  |
| 102 | `new-kvtype-extension-checklist` | Checklist para acrescentar `KvType` novo (encoder byte-identico, `kv_row_bytes`, `kv_load`/`kv_load8`, budget, gate) | docs/quants-inventario.md §4 item 1 linha 262; docs/journal-noite.md:26 | sem numero | a frente KV da noite vai acrescentar `q5_0`/`q4_1`; o unico guard hoje e um `return 0` silencioso | P2 | M |  |
| 103 | `b1-wall-wextra-engine` | Ligar `-Wall -Wextra` nos alvos do motor e do servidor | docs/auditoria-qualidade.md §7.3 linha 1110 | sem numero (1 304 avisos com as flags; 784 `-Wsign-compare` de uma unica linha) | flags de aviso no CMakeLists + corrigir 3 linhas (adiado: ligar antes so produziria ruido); JA APLICADO nesta arvore em CMakeLists.txt:38,40,246 | P3 | V |  |
| 104 | `e7-launch-naming` | Convencao `_launch` e prefixo `d_` inconsistente (32 launchers, 7 sem sufixo, 28 views sem `d_`) | docs/auditoria-qualidade.md §E7 linha 714 | sem numero | renomear 7 launchers e 28 views (adiado: estilo); a assimetria real e que `quantize_q8_1` de producao nao tem launcher | P3 | M |  |
| 105 | `e9-magic-numbers` | Numeros magicos que deveriam ser constantes nomeadas (256 threads, 4096, `+ 8`, `20.0f`, `PF_DIST`) | docs/auditoria-qualidade.md §E9 linha 783 | sem numero | nomear ~8 literais (adiado: estilo) | P3 | M |  |
| 106 | `e10-naming-scales` | Cinco convencoes de enumerador e tres estilos de constante para a mesma coisa | docs/auditoria-qualidade.md §E10 linha 802 | sem numero | escolher um por categoria e anotar a regra no topo de include/rdna4/ (adiado: estilo) | P3 | M |  |
| 107 | `e11-headers-targets-names` | `tokenizer.h` e o unico dos 28 headers sem `#pragma once`; sem CTest; 2 arquivos sem alvo; alvo kebab-case contra arquivo snake_case | docs/auditoria-qualidade.md §E11 linha 814 e linha 823 | sem numero | `#pragma once` na linha 1 (confirmado ausente: include/rdna4/tokenizer.h:1 e comentario) + `enable_testing()`/`add_test()`; o grep por `enable_testing` e `add_test` no CMakeLists volta vazio | P3 | V |  |
| 108 | `e12-broken-crossrefs` | Referencias cruzadas quebradas ou nao verificaveis | docs/auditoria-qualidade.md §E12 linha 840 | sem numero | corrigir citacoes: dequant_row.cuh:60 cita dequant.cuh:286 quando a expressao esta em :288; e a propria auditoria cita `rocm-estudo.md:417` para o `kAttnSplitMin = 2048`, mas a linha verificada nesta arvore e a **429** (`sed -n '429p'` = `static constexpr int kAttnSplitMin = 2048;      // hoje`); o fuzz de 3000 strings nao e verificavel na arvore (confianca media) | P3 | M |  |
| 109 | `h6-hipfree-silent` | 17 `hipFree` silenciosos em `release()` | docs/auditoria-qualidade.md §H6 linha 207 | sem numero (um vazamento de VRAM nao deixa rastro) | acumular os erros num bool e avisar em stderr (nao corrigido; ignorar retorno em destrutor e defensavel) | P3 | V |  |
| 110 | `h7-nodiscard-in-tests` | 422 avisos `nodiscard` ignorados em `tests/` e benches | docs/auditoria-qualidade.md §7.3 linha 1127 | sem numero (0 em include/ e src/) | ruido de teste, nao caminho de producao (19/19 hipMalloc e 41/41 lancamentos de producao ja sao checados) | P3 | V |  |
| 111 | `h9-atoi-diagnostic-knob` | `atoi` em `RD_ATTN_SPLITS` sem checagem (`=abc` vira 0, `=4x` vira 4) | docs/auditoria-qualidade.md §H9 linha 273; include/rdna4/graph.cuh:332 | sem numero | `strtol` com checagem de `end` e intervalo (cosmetico); a auditoria cita graph.cuh:315, nesta arvore a linha e 332 | P3 | V |  |
| 112 | `m2-attn-partials-unwritten` | Parciais da atencao sem escrita quando `head_dim > WPB*32`; o merge lia VRAM nao inicializada | docs/auditoria-qualidade.md §M2 linha 327 | sem numero | CORRIGIDO nesta arvore (include/rdna4/attn.cuh:405, laco strideado com o comentario citando o achado M2) depois de §7.3 ter adiado | P3 | V |  |
| 113 | `info-overhead-1gib` | O orcamento do `info` assume 1,00 GiB de overhead onde o motor usa ~0,40 GiB | docs/medicoes-m5.md:166-169; SPEC.md:84-85 | sem numero (recusa IQ4_XS 32K f16 com need 16,27 GiB contra 15,66 GiB reais) | e um guard pre-voo, nao uma medicao: usar o overhead medido ou imprimir aviso | P3 | V |  |
| 114 | `spec-f16-bf16-whitelist` | `SPEC.md:21` promete `F16`/`BF16` "se aparecerem"; `dtype_from_ggml` rejeita os dois | docs/quants-inventario.md §4 linha 267; SPEC.md:21 | sem numero | corrigir o texto, nao o codigo (o codigo esta certo para estes dois arquivos) | P3 | M |  |
| 115 | `spec-mmvq-params-nonexistent` | `SPEC.md:33` cita `MMVQ_PARAMETERS_RDNA4` e `mmq-config-rdna4.cuh`, que nao existem na arvore, e nao ha GEMM/MMQ | docs/auditoria-qualidade.md §E5 linha 671; SPEC.md:33 | sem numero | corrigir o texto (o caminho em batch e GEMV batched, `matvec_launch_batch`) | P3 | M |  |
| 116 | `spec-mtp-server-nongoal` | `SPEC` §2 lista MTP e servidor HTTP como nao-objetivo e §4 como futuro; ambos existem e sao testados | docs/auditoria-qualidade.md §E5 linha 674; SPEC.md:42-43,106 | sem numero | atualizar SPEC §2 e §4 | P3 | M |  |
| 117 | `split-policy-512` | Politica de splits `kAttnSplitMin` 2048 para 512 | docs/rocm-estudo.md §C linha 324 e §E.4 linha 453 | +15% a 4K f16 (23,95 para 27,54 tok/s com `RD_ATTN_SPLITS=8`); re-medido 23,48 para 26,92 (+14,7%) | 1 linha em graph.cuh; JA APLICADO (include/rdna4/graph.cuh:325 = 512, M8); o doc continua listando como candidato com dono | P3 | V |  |
| 118 | `quantize-once-per-activation` | Tirar as quantizacoes redundantes de ativacao | docs/rocm-estudo.md §C linha 325 e §E.1 linha 376 | ~+2,4% (0,85 ms + 192 gaps); §A.2.1 da teto 1,36-1,5 ms (4,1%) | JA APLICADO por `proj_qq` (M8, +1,3% medido); restam 113 quantizacoes genuinamente necessarias (docs/kv-memoria-desenho.md §3.4 linha 258) | P3 | V |  |
| 119 | `hipmemcpy-pos-by-value` | Passar `pos` por valor para a RoPE e eliminar o `d_pos_` | docs/rocm-estudo.md §E.3 linha 418 e §A.2.9 linha 169 | 0,05 ms (0,15%); tira 16 sincronizacoes do caminho quente | baixo ganho, bit-exato, mexe na assinatura do kernel | P3 | M |  |
| 120 | `rocm-rejected-rows-per-cta` | `ROWS` por CTA (linhas por bloco) | docs/rocm-estudo.md §D.1 linha 338 | 0,99-1,01x sobre os 497 tensores reais (piso de ruido da propria medicao e 1,00x) | rejeitado com dado: e bit-exato e nao vale nada | P3 | M |  |
| 121 | `rocm-rejected-minb` | `__launch_bounds__` / orcamento de registrador (`minb`) | docs/rocm-estudo.md §D.2 linha 341 | 0,82x = 18% mais lento com `minb` em {2,4,6} nos 14 tipos | rejeitado com dado: a ocupacao ja estava no plato de 64 waves/WGP | P3 | M |  |
| 122 | `rocm-rejected-unroll-manual` | Unroll manual com acumulador unico (MLP bit-exata) | docs/rocm-estudo.md §D.3 linha 345 | 1,03x com 2 blocos e 0,92x com 4 | rejeitado com dado: o compilador ja extrai essa paralelidade | P3 | M |  |
| 123 | `rocm-rejected-prefetch-l2` | Prefetch L2 (`__builtin_prefetch` e depois `s_prefetch_data`) | docs/rocm-estudo.md §D.4 linha 348 | 0,98x (o `__builtin_prefetch` compilava para nada em gfx1201) | rejeitado com dado: continua nao pagando | P3 | M |  |
| 124 | `rocm-rejected-ilp-acumuladores` | `ILP` com acumuladores independentes | docs/rocm-estudo.md §D.5 linha 353 | +-3% (resultado do M2); nao re-medido | rejeitado: muda a ordem de soma (nao e livre) e as duas alavancas bit-exatas equivalentes nao pagam | P3 | M |  |
| 125 | `rocm-rejected-wpb32` | `WPB=32` na atencao (o "8 para 32 warps" do commit a830570) | docs/rocm-estudo.md §D.7 linha 359 | no kernel sem split 0,73x a 4K e 1,07-1,19x a 8-64K; com split sempre pior que 8 ou 16 (16 splits a 64K: 0,1111 contra 0,0819 com 8 warps) | rejeitado com dado; e a varredura sem split mede warps redundantes (achado E2) | P3 | M |  |
| 126 | `gqa-warp-prototype-rejected` | Compartilhar K/V entre as cabecas GQA no nivel do warp (prototipo M7) | docs/rocm-estudo.md §D.8 linha 365 | 8-12x mais lento | rejeitado; so reconsiderar com a grade larga dos splits e staging em LDS por CTA | P3 | M |  |
| 127 | `vulkan-dont-dequant-weights` | Nao copiar: dequantizar pesos para buffer f16/f32 antes do matmul | docs/vulkan-vs-hip.md §5.1 linha 660 | sem numero (custaria uma passada de 11 GB de leitura + escrita por token) | decisao: nao copiar | P3 | M |  |
| 128 | `vulkan-dont-occupancy-limiter` | Nao copiar: o `occupancy_limiter` (shared memory inutil para baixar ocupacao) | docs/vulkan-vs-hip.md §5.2 linha 664 | a alavanca equivalente (`__launch_bounds__`/`minb`) foi medida e rejeitada: 0,82x | decisao: nao repetir | P3 | M |  |
| 129 | `vulkan-dont-fp16-acc` | Nao copiar: acumulacao em fp16 (`ACC_TYPE = float16_t`) | docs/vulkan-vs-hip.md §5.3 linha 668 | sem numero (quebra os gates de 1e-6 do M2/M3 e a comparacao de PPL por posicao) | decisao: nao copiar | P3 | M |  |
| 130 | `vulkan-dont-fltmax-softmax` | Nao copiar: truque `-FLT_MAX/2` no softmax e vies `-3*ln2` | docs/vulkan-vs-hip.md §5.4 linha 673 | sem numero (evitam NaN/overflow em fp16 alterando o resultado) | decisao: o `-INFINITY` com guards explicitos e exato e ja foi validado | P3 | M |  |
| 131 | `vulkan-dont-warp-gqa` | Nao copiar: compartilhar K/V entre as cabecas GQA no nivel do warp | docs/vulkan-vs-hip.md §5.5 linha 678 | 8-12x mais lento (prototipo do M7) | decisao: copiar o desenho por workgroup, nao a versao que falhou | P3 | M |  |
| 132 | `vulkan-dont-descriptor-sets` | Nao copiar: maquinaria de descriptor set / push constant | docs/vulkan-vs-hip.md §5.6 linha 681 | sem numero | decisao: os argumentos do `<<<>>>` do HIP ja fazem esse papel sem custo de host | P3 | M |  |
| 133 | `vulkan-dont-single-spirv-switch` | Nao copiar: SPIR-V unico com `switch` de tipo por spec constant | docs/vulkan-vs-hip.md §5.7 linha 684 | sem numero | decisao: os templates C++ dao a mesma especializacao de graca | P3 | M |  |
| 134 | `vulkan-dont-mmq-tile-constants` | Nao copiar: as constantes de tile do `mul_mmq` (BM=BN=64, BK=32, BK_STEP=4) | docs/vulkan-vs-hip.md §5.8 linha 689 | sem numero | copiar a estrutura, nao os numeros: o nosso LDS e 128 KB/WGP e o tile otimo tem de ser medido | P3 | M |  |
| 135 | `vulkan-dont-device-sampling-temp` | Nao fazer: amostragem no device para temperatura/top-p | docs/vulkan-vs-hip.md §5.9 linha 694 | sem numero (exigiria RNG no device e quebraria o criterio de aceite deterministico do M4) | vale so o `argmax` do caminho greedy | P3 | M |  |
| 136 | `vulkan-dont-unified-gemv-gemm` | Nao tentar: um kernel que sirva GEMV e GEMM | docs/vulkan-vs-hip.md §5.10 linha 697 | sem numero | decisao: manter `matvec_kernel_gen` para decode e um kernel proprio para prefill | P3 | M |  |
| 137 | `vulkan-dont-llamacpp-speculation` | Nao copiar: especulacao/MTP do llama.cpp | docs/vulkan-vs-hip.md §5.11 linha 702 | teto de 1,3-1,5x (medido no M8) | trabalho consideravel e arriscado num modelo hibrido recorrente | P3 | M |  |
| 138 | `vulkan-dont-fa-iq4nl` | Nao usar: FA com KV `iq4_nl` | docs/vulkan-vs-hip.md §5.12 linha 704 | sem numero | nao temos esse tipo no cache | P3 | M |  |
| 139 | `vulkan-open-dispatch-cost` | Incerteza: o custo por despacho do Vulkan nao foi medido | docs/vulkan-vs-hip.md §6.2 linha 719 | sem numero (a conta do §2.6 assume que e menor que os nossos 3,5 us) | fechar com `GGML_VK_PERF_LOGGER=1` no `llama-bench` | P3 | M |  |
| 140 | `vulkan-open-vecdot-instructions` | Incerteza: quantas instrucoes tem o corpo do `vec_dot` do Vulkan por 220 bytes | docs/vulkan-vs-hip.md §6.3 linha 723 | sem numero (a comparacao do §3.1 e qualitativa) | fechar sem GPU com `spirv-dis` no `.spv` e contagem do bloco do laco | P3 | M |  |
| 141 | `vulkan-open-dispatch-count` | Incerteza: a contagem de ~1300-1400 despachos do lado Vulkan e estimativa | docs/vulkan-vs-hip.md §6.5 linha 731 | sem numero | contar os nos do grafo (`llama-eval-callback` ou o PERF_LOGGER) | P3 | M |  |
| 142 | `vulkan-baseline-remeasure-flags` | Re-medir os tok/s do baseline com as mesmas flags | docs/vulkan-vs-hip.md §8 linha 811 | sem numero (o `tg8` deu 26,0 contra os 39,7 tok/s do baseline) | o baseline usa `-p 512 -n 128 -r 3` (modelo quente) e um `pp32` de 1 repeticao mede sobrecarga de lancamento, nao vazao | P3 | M |  |
| 143 | `autotuning-rejected-rows-global` | `rows` global | docs/autotuning-gfx1201.md §4.1 linha 226 | 1,010x/1,005x/0,991x/0,988x em 3 corridas independentes | rejeitado com dado: por tipo so rende em dois casos, e `rows=1` em `iq3_s` so e ganho quando nao ha unroll | P3 | M |  |
| 144 | `autotuning-rejected-ilp-global` | `ILP` global | docs/autotuning-gfx1201.md §4.2 linha 229 | 0,82x (ILP 1), 0,76x (2), 0,77x (4) | rejeitado com dado: a alavanca para os tipos de ILP=1 e o UNROLL | P3 | M |  |
| 145 | `autotuning-rejected-unroll4` | `unroll=4` e `unroll=2` nos tipos com ILP>1 | docs/autotuning-gfx1201.md §4.3 linha 231 | 0,917x e 0,98-1,02x | rejeitado com dado | P3 | M |  |
| 146 | `autotuning-rejected-prefetch-real` | Prefetch L2 real (`__builtin_amdgcn_s_prefetch_data`) | docs/autotuning-gfx1201.md §4.4 linha 232 | 0,984x | rejeitado com dado: confirma rocm-estudo §D.4 com o intrinseco que de fato executa | P3 | M |  |
| 147 | `autotuning-rejected-minb-recheck` | `minb` nao re-medido | docs/autotuning-gfx1201.md §4.5 linha 234 | 0,82x ja medido; `--minb` continua no bench para quem quiser repetir | rejeitado com dado | P3 | M |  |
| 148 | `autotuning-rejected-more-splits` | Atencao com mais splits (24/32/48/64) em 4K/16K/64K/131K f16 | docs/autotuning-gfx1201.md §4.6 linha 236 | sempre >= do que 8-16 splits, exceto no KV q4_0 a 131K | rejeitado com dado | P3 | M |  |
| 149 | `autotuning-rejected-prefill-n-reorder` | Prefill N: reordenar a decomposicao do prompt | docs/autotuning-gfx1201.md §4.7 linha 238 | a curva por token cai monotonicamente com N; "nada a mudar" | rejeitado com dado (16,3 ms/tok em N=2 ate 0,87 em N=16) | P3 | M |  |
| 150 | `autotuning-sweep-without-ab` | Varredura de celulas da atencao sem A/B intercalado | docs/autotuning-gfx1201.md §4.8 linha 240 | 15-20% de espalhamento entre celulas independentes; as "vitorias" de 4-5% nao foram aceitas | regra de metodo: quem for re-medir atencao deve usar `--no-sweep --ab ... --cand ...` | P3 | M |  |
| 151 | `discount-mtp-in-vram-account` | Descontar o bloco MTP (0,351 GB) e a linha unica de `token_embd` da conta de VRAM quando `--mtp` esta desligado | docs/medicoes-banda-e-gargalos.md §4.5 linha 507 | 0,351 GB | trivial em main.hip | P3 | M |  |
| 152 | `rocprof-not-installed` | `rocprof`/`rocprofv3`/`omniperf` nao estao instalados: nenhum contador de hardware foi lido | docs/medicoes-banda-e-gargalos.md §6.1 linha 533; docs/gpu-queue.md linha 12 | sem numero (toda atribuicao de latencia vs banda e inferida de tempo e bytes) | instalar as ferramentas ou aceitar a inferencia declarada; a alternativa usada e `s_memrealtime`/`SHADER_CYCLES` | P3 | M |  |
| 153 | `kernel-counters-inside-engine` | Contadores `s_memrealtime`/`SHADER_CYCLES` dentro dos kernels do motor | docs/medicoes-banda-e-gargalos.md §6.2 linha 537 | sem numero | exigiria editar matvec.cuh/attn.cuh/kv.h/nn.cuh, que pertencem a outro agente nesta rodada | P3 | M |  |
| 154 | `attn-unsplit-4k-16k-measure` | Atencao sem split a 4K/16K para medir o ganho do caminho de splits de hoje | docs/medicoes-banda-e-gargalos.md §6.4 linha 542 | sem numero | o `RD_ATTN_SPLITS` do M7 permite, mas nao foi rodado nesta sessao | P3 | M |  |
| 155 | `qwen-packed-f16-attention` | Atencao com f16 empacotado (`v_pk_fma_f16`/`v_dot2_f32_f16`, 1,5x o rate do FMA fp32) para o KV f16 | docs/qwen-kernels.md §5 item 6 linha 409 | desconhecido; a atencao e issue-bound na desquantizacao, nao no FMA, entao pode nao pagar | baixa [hipotese], esforco medio em attn.cuh e kv.h | P3 | M |  |
| 156 | `qwen-gdn-state-traffic` | O estado do GDN: 302 MB/token (~0,9 ms, 2,5%) nao aparece em nenhuma tabela do repo | docs/qwen-kernels.md §4 linha 391 | 302 MB/token de trafego real (48 camadas x 3,1 MB, lidos e escritos) | [minha conta] e trafego real que explica parte do "resto" do estudo; nao e conversao redundante | P3 | M |  |
| 157 | `kv-remove-d2d-prefill-copy` | Tirar a copia D2D do prefill | docs/kv-memoria-desenho.md §5 proposta 9 linha 406; include/rdna4/graph.cuh:976-980 | 1,18 MB por token de prefill mais 48 copias, ~0 no tempo (2 us de banda) | medio (mexe no `ssm_out` batched); bit-exato | P3 | M |  |
| 158 | `kv-paging-block-table` | Paging / block table no KV | docs/kv-memoria-desenho.md §5 proposta 10 linha 407 | 0 para este workload | grande em kv.h + graph.cuh + attn.cuh; so faz sentido junto com prefix cache no servidor | P3 | M |  |
| 159 | `kv-rejected-head-major-layout` | Rejeitado: inverter o layout para head-major | docs/kv-memoria-desenho.md §5 linha 411 | sem numero (destroi a linha de token contigua de 2048 B que da o acesso coalescido de 512 B por warp, e a localidade entre as 6 cabecas) | rejeitado com motivo | P3 | M |  |
| 160 | `kv-rejected-second-copy` | Rejeitado: guardar uma copia f32/f16 alem da quantizada | docs/kv-memoria-desenho.md §5 linha 416 | a 131K seriam +2,25 GiB | rejeitado: custa exatamente a VRAM que o `q4_0` existe para economizar | P3 | M |  |
| 161 | `kv-rejected-fewer-splits` | Rejeitado: reduzir splits para economizar o merge | docs/kv-memoria-desenho.md §5 linha 418 | sair de 8 splits para 1 a 4K custa 23,48 para 26,92 tok/s (+14,7% com 8 splits) | rejeitado: a economia de 16 lancamentos nao paga a perda de paralelismo | P3 | M |  |
| 162 | `kv-rejected-layer-padding` | Rejeitado (por ora): padding entre camadas / entre K e V para evitar conflito de conjunto no L2 | docs/kv-memoria-desenho.md §5 linha 421 | sem numero | nao considerado problema (o laco de camadas e sequencial); se houver prefill em batch com camadas sobrepostas, medir antes de mudar | P3 | M |  |
| 163 | `kv-verify-partials-in-l2` | Verificar se os parciais do split ficam em L2 | docs/kv-memoria-desenho.md §6 linha 435 | 396 KB reutilizados [estimativa: o buffer e 387 KiB contra 8 MiB de L2] | `bench-attn-gpu` com e sem o merge, medindo so o kernel de merge | P3 | M |  |
| 164 | `kv-verify-gqa-6x-in-l2` | Verificar que o 6x do GQA cai no L2 | docs/kv-memoria-desenho.md §6 linha 434 | [hipotese] sustentada pela taxa de 1,35 TB/s medida | contadores de L2 via `bench-attn-gpu` variando o numero de heads por grupo, ou comparar tempo com 4 heads contra 24 na MESMA grade | P3 | M |  |
| 165 | `kv-verify-attn-dram-69ms` | Verificar por medicao que a atencao a 64K sao 19 ms dos quais ~6,9 ms sao DRAM | docs/kv-memoria-desenho.md §6 linha 436 | sem numero (estimativa por aritmetica) | `bench-attn-gpu f16 65536` com tensor sintetico (o bench ja aquece a GPU e isola o kernel) | P3 | M |  |
| 166 | `kv-verify-merge-cost` | Verificar o custo do merge = 16 x gap de despacho | docs/kv-memoria-desenho.md §6 linha 438 | [estimativa] | `bench --start-pos 65000` com e sem merge fundido, via `scripts/check_attn_split.sh` | P3 | M |  |
| 167 | `kv-fuse-kv-epilogue` | Epilogo fundido na projecao K/V (`d_kstage_`/`d_vstage_` f32 para o cache quantizado) | docs/kv-memoria-desenho.md §3.8 linha 238 | 128 KiB lidos por token | fusao dificil: mexe em matvec.cuh | P3 | M |  |
| 168 | `kv-split-q-gate-buffers` | Projetar Q ja com q e gate em buffers separados (evita o `d_proj_` escrito e relido) | docs/kv-memoria-desenho.md §3.9 linha 239 | 48 KB mais 48 KB por camada | sim, mas caro: muda o matvec ou o peso | P3 | M |  |
| 169 | `mtp-rejection-sampling` | Amostragem especulativa correta (rejection sampling com `p_trunk(x)/p_draft(x)` e reamostragem da residual) para temp > 0 | docs/mtp.md §7 linha 267 e §5 linha 224 | sem numero | nao implementado; o CLI hoje recusa `--mtp` com `temp > 0` | P3 | M |  |
| 170 | `mtp-draft-step-cost` | Ajustar o custo do passo de rascunho (2,28 ms para 334,7 MiB de pesos) | docs/mtp.md §7 linha 270 | ~147 GB/s efetivos contra ~600 GB/s da placa: 8 matvecs pequenos mais dois readbacks por passo | se o verify batelado acontecer, esse custo passa a dominar e vale otimizar | P3 | M |  |
| 171 | `mtp-concat-order-knob` | `RD_MTP_CONCAT=he` inverte a ordem do concat | docs/mtp.md §6 linha 260 | sem numero (so para medir) | mantido como knob de diagnostico; a ordem correta foi descoberta assim (96,9% contra 0,0% de aceitacao) | P3 | M |  |
| 172 | `mtp-single-nextn-block` | So um bloco NextN e suportado | include/rdna4/mtp.cuh:261 | sem numero (`MtpHead::init` falha alto em qualquer outro valor de `nextn_predict_layers`) | suportar mais blocos e trabalho novo | P3 | M |  |
| 173 | `prefill-target-150-unreachable` | A meta de >= 150 tok/s nao e alcancavel so com weight sharing | docs/medicoes-m6.md linha 97 | projecao ~63 tok/s, teto estrutural ~145 tok/s | re-escopo medido: exige em batch tudo (norms, atencao, recorrencia GDN, quantizacao) mais um laco interno muito mais barato | P3 | M |  |
| 174 | `attn-per-type-n-retune` | Re-tune por (tipo, N) do matvec batelado | docs/medicoes-m7.md linha 164 | sem numero | "nao e necessario para usabilidade" | P3 | M |  |
| 175 | `iq4xs-long-context-unusable` | O IQ4_XS nao e utilizavel a contexto longo nesta placa | docs/quants-precisao.md §6 linha 258 | falha de `hipMalloc` no `blk.38` a 48K com `q4_0` e com `f16` a 32K | e escolha de arquivo, nao corrigivel por kernel; o ganho de precisao dele existe so ate ~32K | P3 | M |  |
| 176 | `m5-stale-attention-claim` | `docs/medicoes-m5.md:191` descreve a atencao como gargalo ("a 64K um passo de decode custa ~262 ms") | docs/kv-memoria-desenho.md §7.3 linha 458 | sem numero (e o baseline pre-M7; o M7 fechou em 19 ms de atencao) | o texto continua no repositorio sem nota de revisao | P3 | M |  |
| 177 | `m6-l2-label-wrong` | `docs/medicoes-m6.md:22-23` chama os 64 MB de L2 | docs/auditoria-qualidade.md §E5 linha 681 | sem numero | o L2 e 8 MiB e os 64 MB sao o Infinity Cache (o proprio repo diz isso em dois lugares) | P3 | M |  |
| 178 | `m7-split-policy-stale` | `medicoes-m7.md:86` descreve a politica de splits como `keys / 2048` | docs/kv-memoria-desenho.md §7.5 linha 467 | sem numero (o codigo de hoje e `keys / 512`) | corrigir o texto | P3 | M |  |
| 179 | `plan-output-weight-type-wrong` | `PLAN.md:302` diz que `output.weight` e Q6_K no IQ3_S | docs/quants-inventario.md §3.4 linha 224 | sem numero (e `q5_k` nos dois arquivos [gerado]) | corrigir o texto | P3 | M |  |
| 180 | `serve-keepalive` | Keep-alive e pipelining nao implementados (uma requisicao por conexao, `Connection: close`) | include/rdna4/server.h:7; docs/servidor-openai.md linha 125 | sem numero | decisao deliberada: remove uma classe inteira de bugs de pipelining da superficie de review | P3 | M |  |
| 181 | `serve-prefix-kv-reuse` | `--kv-reuse` nao implementado: sem reuso de prefixo/KV entre requisicoes | docs/servidor-openai.md linha 128 | sem numero | exigiria rollback do estado recorrente, possivel so com o state checkpointing do llama.cpp (fora de escopo para a v1); toda requisicao chama `Graph::reset_state()` | P3 | M |  |
| 182 | `serve-reasoning-content` | Sem split de `reasoning_content` | docs/servidor-openai.md linha 132 | sem numero | o texto gerado (incluindo o bloco `<think>`) volta em `content` | P3 | M |  |
| 183 | `serve-tools-logprobs` | Sem tool calls, logprobs, logit_bias, multi-choice nem embeddings | docs/servidor-openai.md linha 135; src/server/serve.hip:844 | sem numero | os campos aceitos e ignorados sao logados com `-v` (a mensagem literal e "accepted but ignored (not implemented)") | P3 | M |  |
| 184 | `serve-one-request-at-a-time` | Uma requisicao por vez; a segunda espera no backlog | docs/servidor-openai.md linha 121 | sem numero | o grafo tem o unico KV cache e o unico estado GDN; uma segunda requisicao concorrente corromperia a primeira | P3 | M |  |
| 185 | `serve-run-3-byte-diff` | `run` e o servidor diferem em ate 3 bytes num caso raro (UTF-8 partido no fim da geracao) | docs/servidor-openai.md linha 136 | ate 3 bytes | os ids sao identicos; o servidor descarta e loga quantos bytes | P3 | M |  |
| 186 | `serve-prefill-client-timeout` | O cliente precisa de timeout generoso porque o prefill nao e batelado (4096 tokens ~= 2,5 min) | docs/servidor-openai.md linha 112 | sem numero | documentado; deixa de ser necessario quando o prefill tiled entrar | P3 | M |  |
| 187 | `serve-check-count-stale` | `docs/servidor-openai.md:202` diz 68 CPU checks, agentes-paralelos diz 70, o arquivo tem 72 sitios CHECK | docs/auditoria-qualidade.md §E12 linha 861 | sem numero | corrigir os dois numeros | P3 | M |  |
| 188 | `paralelos-proj-act-ready-fusions` | `proj()` com `act_ready` e fusao das cadeias pequenas | docs/agentes-paralelos.md linha 83 | sem numero | descritas em rocm-estudo §E e exigem graph.cuh; `act_ready` JA FOI feito (proj_qq, M8), as fusoes nao | P3 | M |  |
| 189 | `build-rocm-min-version` | O piso "ROCm >= 6.4.1" e evidencia externa, nao medida nesta maquina | docs/build-repro.md §2.1 linha 107 | sem numero | nao ha como verificar toolchains antigas sem instalar (proibido na tarefa); "ROCm 7.2.4 + amdclang 22" e a unica combinacao realmente verificada | P3 | M |  |
| 190 | `build-engine-without-llamacpp` | Nao existe caminho "buildar so o motor sem llama.cpp" com o CMakeLists como esta | docs/build-repro.md §4.3 linha 289 | sem numero | o unico `message(FATAL_ERROR)` (CMakeLists.txt:66) roda no configure; contorno sem editar nada e apontar `LLAMA_CPP_BUILD` a um diretorio com um arquivo de 0 byte chamado `libggml-base.so` | P3 | M |  |
| 191 | `build-distro-packages-unverified` | Nomes de pacote Ubuntu/Debian e RHEL/Fedora nao verificados aqui | docs/build-repro.md §5.1 linha 318 | sem numero | evidencia externa (pagina de instalacao da AMD); a documentacao antiga usava outro nome | P3 | M |  |
| 192 | `build-hip-flags-shadowing` | `-DCMAKE_HIP_FLAGS_RELEASE` na linha de comando e silenciosamente ignorado | docs/build-repro.md §6.2 linha 428 | sem numero (o `set()` normal sombreia a variavel de cache; `-O0` perdido) | usar `CMAKE_BUILD_TYPE` ou editar o CMakeLists | P3 | M |  |
| 193 | `build-kernels-static-inline` | Marcar os 21 kernels `static`/`inline` para permitir uma segunda TU | docs/build-repro.md §6.4 linha 490 | sem numero (20 simbolos duplicados no total; 53 `T` exportados no `.so` contra 55 no executavel) | mexeria em 6 headers de outro workstream (a alternativa escolhida foi o `.so`) | P3 | M |  |
| 194 | `build-hex-escape-warning` | O aviso de escape hexadecimal em `tokenizer.cpp:140` | docs/build-repro.md §6.6 linha 504 | sem numero (a string vira 26 bytes e o `e` de "end" desaparece) | corrigido nesta arvore (literais adjacentes mais `static_assert`); `amdclang++` 22 trata como ERRO, entao mover a TU para o lado HIP quebraria o build | P3 | M |  |
| 195 | `plan-review-pending-items` | Pendencias da revisao adversarial do M2 (L1 SPEC F16/BF16, L2 limites de string/array no parser GGUF, L3 pad_to defensivo, L6 tipos ausentes nao contam como falha, L7 tabelas de tuning duplicadas, L8 file_end_exact nao aplicado, L10 sem guard de ncols % qk, L11 tabelas antigas de GB/s, L12 nits) | PLAN.md:178 | sem numero | varias foram fechadas depois (L2 virou o M8 corrigido; L7 virou tuning.h unico); as demais ficam registradas como nao corrigidas | P3 | M |  |
| 196 | `plan-attn-no-tiling-note` | O kernel de atencao continua sem tiling (performance, nao correcao) | PLAN.md:294 | sem numero (262 ms/token em 64K) | fechado pelo M7 (split-KV); o texto e de antes | P3 | M |  |
| 197 | `scripts-readme-future-files` | `scripts/README.md` promete `bench.py` e `smoke.py` como futuros | docs/auditoria-qualidade.md §E5 linha 684; scripts/README.md:8 | sem numero | os equivalentes sao subcomandos mais `check_golden_run.sh`; o README documenta 2 dos 11 scripts | P3 | M |  |
| 198 | `tests-readme-placeholder` | `tests/README.md` diz "a definir em cada milestone" | docs/auditoria-qualidade.md §E5 linha 684 | sem numero | todos os milestones estao fechados e existem 19 checks; documentar | P3 | M |  |
| 199 | `scripts-missing-wikitext-fetch` | `scripts/check_attn_split.sh:37` cita `scripts/get-wikitext-2.sh`, que nao existe na arvore | docs/auditoria-qualidade.md §E5 linha 683; scripts/check_attn_split.sh:37 | sem numero | criar o script ou corrigir a citacao (o gate depende de `reference/`, que e gitignored) | P3 | M |  |
| 200 | `readme-check-graph-env-undocumented` | README documenta `check-graph-gpu` sem `GRAPH_LAST_TOKEN=1`, obrigatorio com dump `-ub 1` | docs/auditoria-qualidade.md §E12 linha 847 | sem numero | documentar a variavel (o briefing de dois agentes ja errou por isso) | P3 | M |  |
| 201 | `fuzz-3000-strings-unverifiable` | O fuzz de "3000 strings aleatorias" nao e verificavel na arvore | docs/auditoria-qualidade.md §E12 linha 857; PLAN.md:316 | sem numero (o corpus commitado tem 205 linhas) | confianca media: pode ter sido um experimento local nao commitado | P3 | M |  |
| 202 | `quant-absolute-weight-error` | O erro absoluto do peso de cada formato (contra o original fp32/torch) | docs/quants-precisao.md §7.1 linha 275 | sem numero (nao existe esse modelo na maquina) | §3 mede a distancia entre dois formatos, que e um limitante superior da soma dos dois erros, nao o erro de cada um | P3 | M |  |
| 203 | `quant-checks-not-rerun` | `check-matvec-gpu`/`check-dequant-gpu` completos nao foram rodados nesta sessao | docs/quants-precisao.md §7.2 linha 279 | sem numero | o M8 os deixou verdes; `check-graph-gpu` estava vermelho por dump de oraculo desatualizado | P3 | M |  |
| 204 | `missing-formats-q4-1-q5-0` | Formatos ausentes nos dois arquivos (`q4_1`, `q5_0`, `bf16`, `fp16`) | docs/quants-precisao.md §7.5 linha 289 | sem numero | nada a medir sem um GGUF que os use | P3 | M |  |
| 205 | `spec-future-grammars` | Gramaticas / constrained decoding (JSON schema etc.) | SPEC.md:49 e SPEC.md:107 | sem numero | nao-goal explicito da v1: o sampler cobre so o §1.4 | P3 | M |  |
| 206 | `spec-future-qwen35moe` | `qwen35moe` e outras familias (Qwen3.5/3.6/4Exp, Llama, Mistral, DeepSeek, multimodais) | SPEC.md:42 e SPEC.md:107 | sem numero | nao-goal explicito | P3 | M |  |
| 207 | `spec-future-mxfp4` | `MXFP4` nativo | SPEC.md:107 | sem numero | fora do union misto dos arquivos UD | P3 | M |  |
| 208 | `spec-future-vision-mmproj` | mmproj de visao (os arquivos `mmproj-F16`/`mmproj-q8_0` ja existem no diretorio de teste) | SPEC.md:107 | sem numero | nao usado por este motor; 927,6 MB e 625,7 MB no disco | P3 | M |  |
| 209 | `spec-future-rust-modules` | Modulos em Rust com a v1 C++ como referencia de comportamento | SPEC.md:107 | sem numero | futuro registrado para nao perder | P3 | M |  |
| 210 | `spec-future-q4km-offload` | `Q4_K_M` com offload | SPEC.md:45 e SPEC.md:107 | sem numero (~16-17 GB nao cabe nos 16 GB) | nao-goal da v1 | P3 | M |  |
| 211 | `spec-nongoal-shards-multigpu` | Shards GGUF, offload CPU/GPU parcial, multi-GPU, tensor parallel | SPEC.md:47 | sem numero | nao-goal explicito | P3 | M |  |
| 212 | `spec-nongoal-other-hw-os` | Outro hardware/OS (NVIDIA, CDNA/Instinct, gfx1100/gfx1200, APUs, Windows) | SPEC.md:46 | sem numero | nao-goal explicito: uma GPU, um OS | P3 | M |  |
| 213 | `readme-output-weight-required` | `output.weight` e obrigatorio (sem fallback de embedding amarrado) | README.md:378 | sem numero | erro barulhento em vez de fallback (decisao consciente da revisao n14 do M3) | P3 | M |  |
| 214 | `readme-warning-hygiene-stale` | README diz que os avisos estao "queued with the attention changes" | README.md:353-355 | sem numero (98 x `-Wsign-compare` e 3 parametros nao usados) | nesta arvore o laco de attn.cuh foi corrigido e os parametros foram marcados `[[maybe_unused]]` (gdn.cuh:60, attn.cuh:412, graph.cuh:644); o README esta velho | P3 | M |  |
| 215 | `readme-prefill-second-look` | "The prefill row deserves a second look" | README.md:227-229 | o gap e real (73 contra 440-1143) e alarga com o comprimento do prompt | analise declarada, nao acao: o prefill longo amortiza overhead por chamada e o nosso andaime por token nao | P3 | M |  |
| 216 | `banda-estudo-rest-mismatch` | O "resto" do inventario §A do estudo nao confere: ~2-3 ms estimado contra ~9,5 ms medidos | docs/medicoes-banda-e-gargalos.md §5 linha 523 | ~9,5 ms (GDN 5,63 mais normas/rope 2,96 mais `act_quant` 0,88) | corrige o estudo: e (a) a `delta_rule` a 152 GB/s (3,97 ms) e (b) ~1440 lancamentos pequenos no piso de despacho (~4,0 ms) | P3 | M |  |
| 217 | `banda-launch-count-mismatch` | Contagem de lancamentos: ~2200 estimado no estudo contra 1940 derivada do codigo | docs/medicoes-banda-e-gargalos.md §5 linha 524 | ~7 ms estimado contra ~4,0 ms medidos | corrige o estudo: a magnitude confere, a contagem nao, e o gap nao e uniforme (497 matvecs de 47 us escondem o proprio despacho) | P3 | M |  |
| 218 | `banda-gtt-contention-caveat` | A contencao da outra fila da GPU invalidou medidas | docs/medicoes-banda-e-gargalos.md §6.6 linha 546 | medianas 15-20% acima da melhor passada; um `check_golden_run.sh` deu 5 casos vermelhos por `hipMalloc` alheio | nada disso e do motor: em janela limpa o mesmo gate da OK; quando as duas corridas divergem, as duas estao reportadas | P3 | M |  |
| 219 | `rng-stream-not-llamacpp` | O RNG do sampler e proprio e deliberadamente NAO e o stream do llama.cpp | include/rdna4/sampler.h:6-16; PLAN.md:321 | sem numero | comparar diretamente com o llama.cpp exigiria reimplementar os internos da distribuicao dele; por isso a comparacao direta e feita no modo greedy | P3 | M |  |
| 220 | `nextn-tensors-read-and-ignored` | Os tensores `nextn_*` do bloco 64 eram lidos e ignorados | docs/exploracao-qwen38.md:17; SPEC.md:107 | sem numero | superado: o MTP existe e e testado; o texto do SPEC §4 continua dizendo "hoje lidos e ignorados" | P3 | M |  |
| 221 | `exploracao-e2-tensor-sensitivity` | E2: sensibilidade a quant por tensor (perplexidade variando 1 tensor por vez) | docs/exploracao-qwen38.md §3 linha 32 | sem numero | "cara, fazer por amostragem"; candidatos `ssm_out`, `attn_qkv`, `ffn_down` | P3 | M |  |
| 222 | `exploracao-e5-draft-mapping` | E5: documentar o formato `nextn_*` do bloco 64 para o futuro MTP, sem escrever codigo | docs/exploracao-qwen38.md §3 linha 35 | sem numero | superado em parte pelo docs/mtp.md | P3 | M |  |
| 223 | `espec-regra-de-saida` | Nada do doc de exploracao vira codigo sem antes virar linha no SPEC.md ou PLAN.md | docs/exploracao-qwen38.md §4 linha 39 | sem numero | regra de processo | P3 | M |  |
| 224 | `hw-inline-asm-for-waitcnt` | `s_wait_loadcnt/dscnt/storecnt` existem mas os builtins nao: usar inline asm | docs/rdna4-gfx1201-hardware-brief.md §3 linha 37 | sem numero | o projeto tem 0 inline asm (objetivo declarado do README); hoje so `s_waitcnt(n)` esta disponivel | P3 | M |  |
| 225 | `hw-coalescing-70-90` | "Coalesced access: 70-90% of peak" e o teto de MLP nao formulado | docs/rdna4-gfx1201-hardware-brief.md §5 linha 57 e linha 58 | 70-90% do pico; [I] 640 GB/s x 226 ns ~= 145 KB em voo | inferido, nao medido: o gargalo declarado e ILP/ocupacao, nao MLP | P3 | M |  |
| 226 | `hw-l2-associativity-unknown` | A associatividade do L2 e o particionamento 2x32 MB nao sao documentados pela AMD | docs/rdna4-gfx1201-hardware-brief.md §1 linha 15 | sem numero | [I] incerto; nenhuma medicao publicada encontrada | P3 | M |  |
| 227 | `new-attn-partial-memset-init` | Aplicar o `hipMemset` defensivo do buffer de parciais no `init` | include/rdna4/graph.cuh (init; o buffer e alocado perto de :535) | sem numero | defense-in-depth recomendado pela auditoria §M2 e nao aplicado; 1 linha | P3 | M |  |
| 228 | `new-warnings-on-test-targets` | Estender `-Wall -Wextra` aos alvos de teste quando o ruido `nodiscard` for tratado | CMakeLists.txt:36-38 (o comentario diz que os alvos de teste ficam fora de proposito) | sem numero (422 avisos) | hoje o sinal dos testes e zero por decisao; os 5 sítios de `hipEvent*` sem checagem podem imprimir tempo nao medido | P3 | M |  |
| 229 | `new-model-path-env` | Variavel de ambiente para o caminho do modelo | docs/build-repro.md §5.3 linha 349 | sem numero | hoje so `-m`; os scripts ja usam `MODEL=${MODEL:-/mnt/raid0/...}` | P3 | M |  |
| 230 | `new-hip-llamacpp-baseline` | Completar o build HIP do llama.cpp e repetir as medidas do baseline nele | docs/baseline-vulkan-iq3s.md linha 37 | sem numero (parado em `hip/hip_fp16.h` nao encontrado no configure) | os numeros HIP do baseline estao declarados como pendentes desde o inicio | P3 | M |  |
| 231 | `new-pp64-reading-rule` | Nao citar `pp64` com tres digitos | docs/baseline-vulkan-iq3s.md linha 45 | 575,00 +- 64,79 contra 440,49 +- 76,70: +30% com o mesmo comando; o `+-` e 11-17% do valor | regra de leitura: usar a referencia estavel `pp512 = 1142,60 +- 29,82` | P3 | V |  |
| 232 | `new-serve-request-log-ids` | O `-v` do servidor imprime os ids do prompt e os gerados: falta um modo estruturado para comparação automatica | docs/servidor-openai.md:31-32 | sem numero | hoje a comparacao com o CLI e feita por md5 de stdout em `check_server.sh` | P3 | M |  |
| 233 | `new-doc-claim-per-launch-floor` | Fixar em um unico lugar o piso de despacho medido (2,20 us enfileirado, 3,5 us estimado no estudo) | docs/medicoes-banda-e-gargalos.md §4.3 linha 444 contra docs/rocm-estudo.md §B.5 linha 299 | 2,20 us medido em cadeia de 100 contra 3,5 us estimado com kernel vazio enfileirado | os dois numeros circulam no repo e alimentam estimativas diferentes (4,0 ms contra 7 ms); consolidar evita repetir a divergencia | P3 | M |  |

---

## 2. Itens desta frente (novos, marcados `rev-`)

Doze itens, todos derivados de achados **verificados** em `docs/adversarial-noite.md`; todos com
`v = V`. Eles estão **dentro** da tabela acima, na posição que o ganho ÷ custo lhes dá, e repetidos
aqui com o achado de origem.

| # | id | item | achado de origem | por que está nessa posição |
|---|---|---|---|---|
| 6 | `rev-check-all-fail-on-missing-gate` | `check_all.sh` falhar (ou imprimir `SKIP` e contar) quando o binário de um gate não existe | F3 | custo de 4 linhas de shell, e o efeito é o relatório de manhã mentir sobre 10+15 gates; provado movendo `build/check-tuning` |
| 14 | `rev-check-tuning-effective-values` | imprimir no `check-tuning` os valores **efetivos** dos consumidores (`attn_split_wpb(2)/(16)`, `attn_splits_for(4096)`, `MtShape`) | F1, F10 | fecha a classe "o gate fica verde e a constante deixa de ser usada"; hoje o `WPB` largo é o literal `16` em `attn.cuh` |
| 15 | `rev-attn-wide-branch-gate` | gate de PPL para a ponta larga: ctx 8192/16384, `RD_ATTN_SPLITS` 15 (`WPB=8`) contra 16 (`WPB=16`) | F5 | é o único jeito de saber se 16 splits × 16 warps preservam os números em texto real; o desenho já existe (eu tentei, fiquei na fila) |
| 26 | `rev-kv-q8-0-vs-q4-0-ab` | re-medir `q8_0` × `q4_0` a 64K em A/B intercalado na mesma janela | F11.1 | hoje há três números para a mesma configuração e **zero** A/B intercalado; o delta publicado é menor que a variabilidade entre sessões |
| 30 | `rev-readme-phase-table-partition` | corrigir a partição da tabela "Where a token goes" | F11.2 | a soma das linhas (37,87) não é o total declarado (36,0) porque o LM head entra em duas linhas e o sampler host entra num total de dispositivo |
| 31 | `rev-readme-bandwidth-after-fix` | re-derivar as linhas de banda do README depois do conserto do `bench` | F4, F11.3 | `352 GB/s` vira ~325 GB/s e `56%` vira `51%` da roofline; "402-421 GB/s medidos" é estimativa de replay |
| 38 | `rev-delete-required-bytes` | apagar as duas sobrecargas mortas de `required_bytes()` | F8 | 12 linhas; a de um tipo só contém a fórmula que o `M4` provou errada, com o nome mais natural de chamar |
| 39 | `rev-kv-fill-q4-0-block-scale` | `kv_fill_kernel` `Q4_0`: reduzir o máximo assinado por warp e publicar `b->d` de uma lane só | F7 | hoje 16 lanes correm na escala do mesmo bloco e o "padrão determinístico" não é determinístico nem um bloco `q4_0` válido |
| 40 | `rev-hardening-git-archive-rev` | `check_hardening.sh`: aceitar commit-ish e extrair com `git archive`; passar o rev no `check_all` | F12 | mantém a coluna "antes" reproduzível para sempre (o worktree pré-correção não existe mais); eu reproduzi as 5 linhas assim |
| 45 | `rev-profiler-robustness` | profiler: macro avalia o argumento 2×, eventos sem `hipEventDestroy`, `on=false` silencioso | F14 | são arestas de um hook que **é** inerte (verifiquei em todos os sítios); nenhum número de produção depende disso hoje |
| 46 | `rev-rocm-estudo-f1-nonregression-note` | corrigir o argumento de não-regressão "≥8 splits roda o mesmo caminho de antes" | F13 | depois da regra de duas pontas esse argumento deixou de valer para ≥16 splits, e ele é citado como controle |
| 47 | `rev-gguf-remaining-guard-nonseekable` | registrar/checar que a metade "cabe no resto do arquivo" do `M8` fica inativa se `ftell` falhar | F12/falsos positivos | observação de robustez, não achado: com arquivo regular os dois freios valem |

---

## 3. Contradições entre documentos (registradas, **não** resolvidas)

| # | contradição | onde |
|---|---|---|
| 1 | **`q3_k` linearidade do `dp4a`**: `~0,15-0,2 ms (+0,5%)` contra `1,31 → ~0,7 ms (+1,7%)` (e `+1,4-1,7%` num terceiro doc) | `docs/rocm-estudo.md:328` × `:258` × `docs/qwen-kernels.md:408` × `docs/medicoes-banda-e-gargalos.md:410` |
| 2 | **Quantizações redundantes**: `305 lançamentos, 192 redundantes` × `497 projeções, 240 redundantes / 257 no pós-M8` × `113 genuinamente necessárias` | `docs/rocm-estudo.md:75` × `docs/vulkan-vs-hip.md:767` × `docs/kv-memoria-desenho.md:258` |
| 3 | **MTP KV cache**: `8 MiB a 4K (2 MB por 1K)` = **uma** cache × o código aloca **duas** (`alloc_kv`) ⇒ 16 MiB a 4K, 256 MiB a 64K | `docs/mtp.md:269` × `include/rdna4/mtp.cuh:365-372` (`docs/auditoria-qualidade.md` §E5 já apontou) |
| 4 | **`q8_0` a 64K**: "não medido neste trabalho, é o teste #1 da próxima sessão" × o mesmo documento mede 19,87 ms contra 21,03 | `docs/medicoes-banda-e-gargalos.md:386` × `:485` |
| 5 | **Piso de despacho**: `2,20 µs` medido em cadeia de 100 × `3,5 µs` estimado com kernel vazio — usados em contas diferentes (4,0 ms × 7 ms) | `docs/medicoes-banda-e-gargalos.md:444` × `docs/rocm-estudo.md:299` |
| 6 | **Contagem de lançamentos**: `~2200` × `1940` | `docs/rocm-estudo.md:294` × `docs/medicoes-banda-e-gargalos.md:524`, `docs/vulkan-vs-hip.md:774` |
| 7 | **`f16` × `q4_0` a 64K**: "f16 é ~38% mais rápido" × "~6%" × **`18,91 tok/s` retratado como não reprodutível** e ainda citado em 4 documentos | `docs/medicoes-m5.md:135` × `docs/medicoes-m7.md:107-108` × `docs/medicoes-m7.md:116-128` (retratação) × `docs/kv-memoria-desenho.md:335,373`, `docs/vulkan-vs-hip.md:495,628`, `docs/auditoria-qualidade.md:657` (citações) |
| 8 | **"32 warps por CTA"** ainda afirmado no `SPEC.md:74` × o código lê `8` de `tuning.h` | `SPEC.md:74` × `include/rdna4/attn.cuh:85` (`docs/medicoes-m7.md:29-36` já registrou a correção) |
| 9 | **Tamanhos do bloco MTP e do embedding**: `42,7 MB` / `556 MB` × `351 MB` / `546 MB` medidos | `docs/rocm-estudo.md` §A.2.6 × `docs/kv-memoria-desenho.md:450` |
| 10 | **ROCm mínimo**: "≥ 6.4.1 (evidência externa)" × "7.2.4 + amdclang 22 é o único verificado" — o SOP não diz qual vale | `docs/build-repro.md` §2.1 |
| 11 | **Número de checagens do servidor**: `68` × `70` × `72` sítios `CHECK*` no fonte | `docs/servidor-openai.md:202` × `docs/agentes-paralelos.md:55` × `tests/check_server_http.cpp` |

Além dessas, os achados `F11` (números do README contra os docs, 5 de 12 com problema) e `F13`
(comentários que descrevem código que não existe) são contradições **documento × código**, com
`file:line` em `docs/adversarial-noite.md`.

---

## 4. O que este backlog **não** cobre (e por quê)

- **Itens que exigem uma decisão de produto**, não de engenharia: a lista de não-objetivos do v1
  (`README.md:28-33`, `SPEC.md` §2). Não estão aqui porque não são follow-ups de trabalho, são
  escopo.
- **Números de GPU desta noite** (as seis frentes que estão rodando agora): o backlog foi extraído da
  árvore no estado `e48f3c4`; o que os outros agentes medirem esta noite muda o *ganho declarado* de
  vários itens P1/P2 (por exemplo, se `q5_0`/`q4_1` entrarem, `kv-q5-0-q4-1-implement` sai da lista e
  `q8-0-kv-default-above-56k` muda de justificativa). Re-checar depois dos merges é exatamente o item
  `new-recheck-deferred-after-merges` (posição 11).
- **Medições**: 24 dos itens são P1 e todos precisam de placa; este backlog diz *o que* medir e *com
  que número* o documento justifica, não substitui a medida nova (`new-audit-static-only-caveat`).
- **Os 196 itens com `v = M`**: foram extraídos mecanicamente com grep por linha (não inventei
  nenhum ponteiro), mas eu não os re-li um a um. Os 39 com `v = V` são os que sustentam o topo do
  ranking e o meu próprio trabalho.

---

## Fechado/refutado em 14/09 08:40 (depois do fechamento da noite) — `docs/journal-lote.md`

- **FECHADO COM CAUSA**: `matvec_kernel_batch` (item de maior valor do backlog). O modelo medido
  é `pass_ms ≈ 13,9 + 6,04·N` sobre os 497 tensores reais: o peso amortiza, o token não. O custo
  marginal de 6,04 ms/token é 85,6 % do prefill e explica o multiplicador do MTP
  (`2·aceitação/1,12`). Ferramenta nova: `bench-matvec-shapes-gpu --batch/--batch-unroll/--batch-cap`.
- **REFUTADO (não voltar sem medida nova)**: (a) staging de ativação em LDS como alavanca nº 2 do
  `journal-kernels.md` §10 — o controle `act_stride=0` (16 tokens na MESMA linha residente na L1)
  dá 95,0 %, não metade; (b) `UNROLL` bit-exato no lote — 62,61/62,96/63,18 ms (N=8) e
  110,17/**116,87**/111,44 ms (N=16) contra 110,70 do shipping, ou seja 5,6 % PIOR; (c) transbordo
  de registrador — `localSizeBytes = 0` em 14 tipos × N ∈ {2,4,8,16}.
- **NOVO, nomeado com o número**: custo marginal por token por tipo de quantização (filtro
  `--only-dt` no bench). Se o custo marginal for proporcional ao número de *elementos* (volume de
  ativação = `Σ nrows×K` ≈ 30 GB por token), `iq1_s` (1,5 bit/peso) custa ~2,3× mais por byte que
  `iq3_s`; se for proporcional aos *bytes*, custa igual. É o experimento que separa "L2/IC
  saturada pelas cargas de ativação" de "throughput de outro recurso".
- **NOVO, forma aritmética**: tile de registrador em M (`RM` linhas por thread — 1 carga de
  ativação para RM linhas; bit-exato pela mesma razão que `ROWS` é livre) e, como rota longa,
  `WMMA i32_16x16x16_iu8` (existe no gfx1201; 4096 MACs por instrução contra 4 do dp4a).
- **FALTOU**: profiler (`rocprof`/`omniperf` não estão instalados) — sem ele a separação
  "throughput saturado × latência com ocupação máxima" fica por conta de experimentos dentro do
  kernel, e a sondagem de duas streams só mostra que não há slot de CTA livre (1,93× de 2,00×).

---

## P0 DE CORRETUDE — RESOLVIDO em 14/09 (tarde): o KV do alvo quebrava o prefill

- **`K=q5_0`/`V=q4_1` faz PAGE FAULT no prefill em lote** (`Memory access fault ... Page not present
  or supervisor privilege`). Reproduzido por mim com o binário de produção
  (`rdna4-infer bench --prefill 64 --cache-type-k q5_0 --cache-type-v q4_1`); passa com
  `--cache-type-v q4_0` (72,09 tok/s). Matriz completa da frente I: falha **`K ∈ {f16, q5_0}` com
  `V = q4_1`**; passam `q4_0/q4_1`, `q8_0/q4_1`, `q4_1/q4_1`, `q5_0/q5_0`, `q5_0/f16`, `q4_1/q5_0`.
- **Escopo**: só o caminho EM LOTE (decode com `q5_0/q4_1` funciona, 23,10 tok/s; falha já com
  `--prefill 16`). `bench-attn-gpu q5_0` quebra em `t=256` **sem** escrita de KV ⇒ o suspeito nº 1
  passa a ser a leitura da atenção em lote, e o nº 2 o `kv_write_batch`, que passa o **mesmo `width`**
  para K e V com tamanhos de linha diferentes (`graph.cuh:873-877`).
- **Por que é P0**: é exatamente o par que a rodada noturna recomendou (K `q5_0`, V `q4_1`) para os
  131K — o alvo do usuário. Corrigir ou vetar explicitamente; não deixar como está.
- **RESOLVIDO**: `kv_write_batch` (`graph.cuh:870`) e a leitura de V na atenção em lote
  (`graph.cuh:1083`) usavam `kv_bytes_` (stride de K) na V, cujo cache é alocado com
  `n_attn * kv_bytes_v_` — overflow de heap quando a linha de K > a de V (f16/q4_1: 86,5 MB;
  q5_0/q4_1: 3,9 MB; **q8_0/q4_1: 27,5 MB, que passava corrompendo memória alheia**). Corrigido
  para `kv_bytes_v_` nos dois sítios. **Gate novo** `scripts/check_kvbatch.sh` (5 pares, exige
  bit-exatidão) ligado ao `check_all.sh`; 5/5 OK. Alvo medido: `q5_0`/`q4_1` a 104,61 tok/s de
  prefill e 28,60 de decode.
